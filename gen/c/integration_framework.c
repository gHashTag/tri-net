/* ============================================================================
   Generated from t27 spec: integration_framework
   DO NOT EDIT - generated by t27c gen-c
   phi^2 + 1/phi^2 = 3 | TRINITY
   ============================================================================ */

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#ifndef INTEGRATION_FRAMEWORK_H
#define INTEGRATION_FRAMEWORK_H

/* -------------------------------------------------------
   Constants
   ------------------------------------------------------- */

#define MAX_MODULES 16
#define MAX_MESSAGES 32
#define MAX_EVENTS 64
#define INTEGRATION_VERSION 1
#define TYPE_NETWORK 0
#define TYPE_TESTING 1
#define type_documentation 2
#define TYPE_VISUALIZATION 3
#define TYPE_SIMULATION 4
#define TYPE_PROFILING 5
#define STATUS_IDLE 0
#define STATUS_ACTIVE 1
#define STATUS_BUSY 2
#define STATUS_ERROR 3
#define STATUS_OFFLINE 4
#define MSG_DATA 0
#define MSG_CONTROL 1
#define MSG_STATUS 2
#define MSG_ERROR 3
#define MSG_EVENT 4
#define EVENT_MODULE_LOADED 0
#define EVENT_MODULE_UNLOADED 1
#define EVENT_TEST_COMPLETED 2
#define EVENT_SIMULATION_STEP 3
#define EVENT_VISUALIZATION_UPDATE 4
#define SEVERITY_INFO 0
#define SEVERITY_WARNING 1
#define SEVERITY_ERROR 2
#define SEVERITY_CRITICAL 3
#define RESOURCE_CPU 0
#define RESOURCE_MEMORY 1
#define RESOURCE_BANDWIDTH 2
#define RESOURCE_STORAGE 3

/* -------------------------------------------------------
   Function prototypes
   ------------------------------------------------------- */

uint32_t create_module_registration(uint32_t module_id, uint32_t module_type, uint32_t priority, uint32_t status);
uint32_t get_registered_module_id(uint32_t registration);
uint32_t get_registered_module_type(uint32_t registration);
uint32_t get_registered_module_priority(uint32_t registration);
uint32_t get_registered_module_status(uint32_t registration);
uint32_t create_integration_message(uint32_t msg_id, uint32_t source, uint32_t dest, uint32_t msg_type);
uint32_t get_integration_message_id(uint32_t message);
uint32_t get_integration_message_source(uint32_t message);
uint32_t get_integration_message_dest(uint32_t message);
uint32_t get_integration_message_type(uint32_t message);
uint32_t send_message(* modules, uint32_t message);
uint32_t receive_message(* messages, uint32_t message_count, uint32_t module_id);
uint32_t create_event(uint32_t event_id, uint32_t event_type, uint32_t source, uint32_t data);
uint32_t get_event_id(uint32_t event);
uint32_t get_event_type(uint32_t event);
uint32_t get_event_source(uint32_t event);
uint32_t get_event_data(uint32_t event);
uint32_t subscribe_to_event(uint32_t module_id, uint32_t event_type, * subscriptions);
uint32_t publish_event(uint32_t event, * subscriptions, * modules);
uint32_t create_state_sync(uint32_t module_id, uint32_t state_version, uint32_t state_data, uint32_t checksum);
uint32_t get_sync_module_id(uint32_t sync);
uint32_t get_sync_state_version(uint32_t sync);
uint32_t get_sync_state_data(uint32_t sync);
uint32_t get_sync_checksum(uint32_t sync);
uint32_t synchronize_states(* modules, uint32_t module_count, uint32_t sync_requests);
uint32_t create_error_propagation(uint32_t source_id, uint32_t error_code, uint32_t severity, uint32_t timestamp);
uint32_t get_error_source(uint32_t error);
uint32_t get_error_code(uint32_t error);
uint32_t get_error_severity(uint32_t error);
uint32_t get_error_timestamp(uint32_t error);
uint32_t propagate_error(uint32_t error, * modules, uint32_t module_count);
uint32_t load_module(uint32_t module_id, uint32_t module_type, uint32_t priority, * modules);
uint32_t unload_module(uint32_t module_id, * modules);
uint32_t create_dependency(uint32_t module_id, uint32_t depends_on, uint32_t dependency_type, uint32_t required);
uint32_t get_dependency_module_id(uint32_t dep);
uint32_t get_dependency_depends_on(uint32_t dep);
uint32_t get_dependency_type(uint32_t dep);
uint32_t get_dependency_required(uint32_t dep);
uint32_t check_dependencies(uint32_t module_id, * dependencies, * loaded_modules, uint32_t module_count);
uint32_t create_resource_request(uint32_t module_id, uint32_t resource_type, uint32_t amount, uint32_t priority);
uint32_t get_resource_request_module(uint32_t req);
uint32_t get_resource_request_type(uint32_t req);
uint32_t get_resource_request_amount(uint32_t req);
uint32_t get_resource_request_priority(uint32_t req);
uint32_t allocate_resources(* requests, uint32_t request_count, uint32_t available_resources);
uint32_t create_integration_report(uint32_t loaded_modules, uint32_t active_messages, uint32_t events_processed, uint32_t errors_handled);
uint32_t generate_integration_stats(* modules, uint32_t module_count, * messages, uint32_t message_count, * events, uint32_t event_count);
uint32_t validate_integration_health(* modules, uint32_t module_count);

/* -------------------------------------------------------
   Function implementations
   ------------------------------------------------------- */

uint32_t create_module_registration(uint32_t module_id, uint32_t module_type, uint32_t priority, uint32_t status) {
    return (((((module_id & 0xFF) << 24) | ((module_type & 0xF) << 20)) | ((priority & 0xF) << 16)) | (status & 0xFFFF));
}

uint32_t get_registered_module_id(uint32_t registration) {
    return ((registration >> 24) & 0xFF);
}

uint32_t get_registered_module_type(uint32_t registration) {
    return ((registration >> 20) & 0xF);
}

uint32_t get_registered_module_priority(uint32_t registration) {
    return ((registration >> 16) & 0xF);
}

uint32_t get_registered_module_status(uint32_t registration) {
    return (registration & 0xFFFF);
}

uint32_t create_integration_message(uint32_t msg_id, uint32_t source, uint32_t dest, uint32_t msg_type) {
    return (((((msg_id & 0xFF) << 24) | ((source & 0xFF) << 16)) | ((dest & 0xFF) << 8)) | (msg_type & 0xFF));
}

uint32_t get_integration_message_id(uint32_t message) {
    return ((message >> 24) & 0xFF);
}

uint32_t get_integration_message_source(uint32_t message) {
    return ((message >> 16) & 0xFF);
}

uint32_t get_integration_message_dest(uint32_t message) {
    return ((message >> 8) & 0xFF);
}

uint32_t get_integration_message_type(uint32_t message) {
    return (message & 0xFF);
}

uint32_t send_message(* modules, uint32_t message) {
    while ((i < MAX_MODULES)) {
        if ((module_id == dest)) {
            if (((status == STATUS_ACTIVE) || (status == STATUS_BUSY))) {
                return 1;
            } else {
                return 0;
            }
        }
        i = (i + 1);
    }
    return 0;
}

uint32_t receive_message(* messages, uint32_t message_count, uint32_t module_id) {
    while ((i < message_count)) {
        if ((dest == module_id)) {
            return i;
        }
        i = (i + 1);
    }
    return MAX_MESSAGES;
}

uint32_t create_event(uint32_t event_id, uint32_t event_type, uint32_t source, uint32_t data) {
    return (((((event_id & 0xFF) << 24) | ((event_type & 0xF) << 20)) | ((source & 0xFF) << 12)) | (data & 0xFFF));
}

uint32_t get_event_id(uint32_t event) {
    return ((event >> 24) & 0xFF);
}

uint32_t get_event_type(uint32_t event) {
    return ((event >> 20) & 0xF);
}

uint32_t get_event_source(uint32_t event) {
    return ((event >> 12) & 0xFF);
}

uint32_t get_event_data(uint32_t event) {
    return (event & 0xFFF);
}

uint32_t subscribe_to_event(uint32_t module_id, uint32_t event_type, * subscriptions) {
    while ((i < MAX_EVENTS)) {
        if ((subscriptions[i] == 0)) {
            subscriptions[i] = create_event(subscription_id, event_type, module_id, 0);
            return 1;
        }
        i = (i + 1);
    }
    return 0;
}

uint32_t publish_event(uint32_t event, * subscriptions, * modules) {
    while ((i < MAX_EVENTS)) {
        if ((sub_event_type == event_type)) {
            if ((send_message(modules, msg) == 1)) {
                notified_count = (notified_count + 1);
            }
        }
        i = (i + 1);
    }
    return notified_count;
}

uint32_t create_state_sync(uint32_t module_id, uint32_t state_version, uint32_t state_data, uint32_t checksum) {
    return (((((module_id & 0xFF) << 24) | ((state_version & 0xFF) << 16)) | ((state_data & 0xFF) << 8)) | (checksum & 0xFF));
}

uint32_t get_sync_module_id(uint32_t sync) {
    return ((sync >> 24) & 0xFF);
}

uint32_t get_sync_state_version(uint32_t sync) {
    return ((sync >> 16) & 0xFF);
}

uint32_t get_sync_state_data(uint32_t sync) {
    return ((sync >> 8) & 0xFF);
}

uint32_t get_sync_checksum(uint32_t sync) {
    return (sync & 0xFF);
}

uint32_t synchronize_states(* modules, uint32_t module_count, uint32_t sync_requests) {
    while ((i < module_count)) {
        if (((status == STATUS_ACTIVE) && (sync_requests > 0))) {
            synced_count = (synced_count + 1);
        }
        i = (i + 1);
    }
    return synced_count;
}

uint32_t create_error_propagation(uint32_t source_id, uint32_t error_code, uint32_t severity, uint32_t timestamp) {
    return (((((source_id & 0xFF) << 24) | ((error_code & 0xFF) << 16)) | ((severity & 0xF) << 12)) | (timestamp & 0xFFF));
}

uint32_t get_error_source(uint32_t error) {
    return ((error >> 24) & 0xFF);
}

uint32_t get_error_code(uint32_t error) {
    return ((error >> 16) & 0xFF);
}

uint32_t get_error_severity(uint32_t error) {
    return ((error >> 12) & 0xF);
}

uint32_t get_error_timestamp(uint32_t error) {
    return (error & 0xFFF);
}

uint32_t propagate_error(uint32_t error, * modules, uint32_t module_count) {
    while ((i < module_count)) {
        if ((severity == SEVERITY_CRITICAL)) {
            if ((send_message(modules, msg) == 1)) {
                notified_count = (notified_count + 1);
            }
        } else if (((severity == SEVERITY_WARNING) || (severity == SEVERITY_ERROR))) {
            if (((module_type == TYPE_TESTING) || (module_type == TYPE_SIMULATION))) {
                if ((send_message(modules, msg) == 1)) {
                    notified_count = (notified_count + 1);
                }
            }
        }
        i = (i + 1);
    }
    return notified_count;
}

uint32_t load_module(uint32_t module_id, uint32_t module_type, uint32_t priority, * modules) {
    while ((i < MAX_MODULES)) {
        if ((get_registered_module_id(modules[i]) == 0)) {
            modules[i] = create_module_registration(module_id, module_type, priority, STATUS_IDLE);
            return 1;
        }
        i = (i + 1);
    }
    return 0;
}

uint32_t unload_module(uint32_t module_id, * modules) {
    while ((i < MAX_MODULES)) {
        if ((registered_id == module_id)) {
            modules[i] = 0;
            return 1;
        }
        i = (i + 1);
    }
    return 0;
}

uint32_t create_dependency(uint32_t module_id, uint32_t depends_on, uint32_t dependency_type, uint32_t required) {
    return (((((module_id & 0xFF) << 24) | ((depends_on & 0xFF) << 16)) | ((dependency_type & 0xF) << 12)) | (required & 0xFFF));
}

uint32_t get_dependency_module_id(uint32_t dep) {
    return ((dep >> 24) & 0xFF);
}

uint32_t get_dependency_depends_on(uint32_t dep) {
    return ((dep >> 16) & 0xFF);
}

uint32_t get_dependency_type(uint32_t dep) {
    return ((dep >> 12) & 0xF);
}

uint32_t get_dependency_required(uint32_t dep) {
    return (dep & 0xFFF);
}

uint32_t check_dependencies(uint32_t module_id, * dependencies, * loaded_modules, uint32_t module_count) {
    while ((i < MAX_MODULES)) {
        if ((dep_module_id == module_id)) {
            if ((required == 1)) {
                while ((j < module_count)) {
                    if ((loaded_id == depends_on)) {
                        found = 1;
break;
                    }
                    j = (j + 1);
                }
                if ((found == 0)) {
                    satisfied = 0;
                }
            }
        }
        i = (i + 1);
    }
    return satisfied;
}

uint32_t create_resource_request(uint32_t module_id, uint32_t resource_type, uint32_t amount, uint32_t priority) {
    return (((((module_id & 0xFF) << 24) | ((resource_type & 0xF) << 20)) | ((amount & 0xFF) << 12)) | (priority & 0xFFF));
}

uint32_t get_resource_request_module(uint32_t req) {
    return ((req >> 24) & 0xFF);
}

uint32_t get_resource_request_type(uint32_t req) {
    return ((req >> 20) & 0xF);
}

uint32_t get_resource_request_amount(uint32_t req) {
    return ((req >> 12) & 0xFF);
}

uint32_t get_resource_request_priority(uint32_t req) {
    return (req & 0xFFF);
}

uint32_t allocate_resources(* requests, uint32_t request_count, uint32_t available_resources) {
    while ((i < request_count)) {
        total_requested = (total_requested + amount);
        i = (i + 1);
    }
    if ((total_requested <= available_resources)) {
        return total_requested;
    } else {
        while (((j < request_count) && (allocated < available_resources))) {
            if (((priority > 7) && ((allocated + amount) <= available_resources))) {
                allocated = (allocated + amount);
                allocated_count = (allocated_count + 1);
            }
            j = (j + 1);
        }
        return allocated;
    }
}

uint32_t create_integration_report(uint32_t loaded_modules, uint32_t active_messages, uint32_t events_processed, uint32_t errors_handled) {
    return (((((loaded_modules & 0xFF) << 24) | ((active_messages & 0xFF) << 16)) | ((events_processed & 0xFF) << 8)) | (errors_handled & 0xFF));
}

uint32_t generate_integration_stats(* modules, uint32_t module_count, * messages, uint32_t message_count, * events, uint32_t event_count) {
    while ((i < module_count)) {
        if (((status == STATUS_ACTIVE) || (status == STATUS_BUSY))) {
            active_modules = (active_modules + 1);
        }
        i = (i + 1);
    }
    while ((j < message_count)) {
        active_messages = (active_messages + 1);
        j = (j + 1);
    }
    while ((k < event_count)) {
        events_processed = (events_processed + 1);
        k = (k + 1);
    }
    return create_integration_report(active_modules, active_messages, events_processed, errors_handled);
}

uint32_t validate_integration_health(* modules, uint32_t module_count) {
    while ((i < module_count)) {
        if ((status == STATUS_ACTIVE)) {
            active_count = (active_count + 1);
        } else if ((status == STATUS_ERROR)) {
            error_count = (error_count + 1);
        }
        i = (i + 1);
    }
    if ((module_count > 0)) {
        active_percentage = ((active_count * 100) / module_count);
    }
    return (((active_percentage & 0xFF) << 24) | ((error_count & 0xFF) << 16));
}

#endif /* INTEGRATION_FRAMEWORK_H */
