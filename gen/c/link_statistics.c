/* ============================================================================
   Generated from t27 spec: LinkStatistics
   DO NOT EDIT - generated by t27c gen-c
   phi^2 + 1/phi^2 = 3 | TRINITY
   ============================================================================ */

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <assert.h>

#ifndef LINKSTATISTICS_H
#define LINKSTATISTICS_H

/* -------------------------------------------------------
   Function prototypes
   ------------------------------------------------------- */

uint16_t get_sent(uint32_t stats);
uint16_t get_recv(uint32_t stats);
uint32_t inc_sent(uint32_t stats);
uint32_t inc_recv(uint32_t stats);
uint32_t reset(void);

/* -------------------------------------------------------
   Function implementations
   ------------------------------------------------------- */

uint16_t get_sent(uint32_t stats) {
    return ((uint16_t)((stats & 0xFFFF)));
}

uint16_t get_recv(uint32_t stats) {
    return ((uint16_t)(((stats >> 16) & 0xFFFF)));
}

uint32_t inc_sent(uint32_t stats) {
    return (stats + 1);
}

uint32_t inc_recv(uint32_t stats) {
    return (stats + 0x10000);
}

uint32_t reset(void) {
    return 0;
}

/* -------------------------------------------------------
   Tests
   ------------------------------------------------------- */

void test_inc_sent_increments(void) {
    s1 = reset();
    s2 = inc_sent(s1);
    assert((get_sent(s2) == 1), "inc works");
}

void test_inc_recv_increments(void) {
    s1 = reset();
    s2 = inc_recv(s1);
    assert((get_recv(s2) == 1), "inc works");
}

void test_both_counters(void) {
    s1 = reset();
    s2 = inc_sent(s1);
    s3 = inc_recv(s2);
    assert((get_sent(s3) == 1), "sent");
    assert((get_recv(s3) == 1), "recv");
}

void test_reset_clears(void) {
    s1 = reset();
    s2 = inc_sent(s1);
    s3 = reset();
    assert((get_sent(s3) == 0), "clears");
}


#endif /* LINKSTATISTICS_H */
