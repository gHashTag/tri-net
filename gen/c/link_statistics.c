/* ============================================================================
   Generated from t27 spec: LinkStatistics
   DO NOT EDIT - generated by t27c gen-c
   phi^2 + 1/phi^2 = 3 | TRINITY
   ============================================================================ */

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <assert.h>
#define t27_assert(c, m) do { if (!(c)) { __builtin_trap(); } } while (0)

#ifndef LINKSTATISTICS_H
#define LINKSTATISTICS_H

/* -------------------------------------------------------
   Function prototypes
   ------------------------------------------------------- */

uint16_t get_sent(uint32_t stats);
uint16_t get_recv(uint32_t stats);
uint32_t inc_sent(uint32_t stats);
uint32_t inc_recv(uint32_t stats);
uint32_t reset(void);

/* -------------------------------------------------------
   Function implementations
   ------------------------------------------------------- */

uint16_t get_sent(uint32_t stats) {
    return ((uint16_t)((stats & 0xFFFF)));
}

uint16_t get_recv(uint32_t stats) {
    return ((uint16_t)(((stats >> 16) & 0xFFFF)));
}

uint32_t inc_sent(uint32_t stats) {
    return (stats + 1);
}

uint32_t inc_recv(uint32_t stats) {
    return (stats + 0x10000);
}

uint32_t reset(void) {
    return 0;
}

/* -------------------------------------------------------
   Tests
   ------------------------------------------------------- */

void test_inc_sent_increments(void) {
    uint64_t s1 = reset();
    (void)s1;
    uint64_t s2 = inc_sent(s1);
    (void)s2;
    t27_assert((get_sent(s2) == 1), "inc works");
}

void test_inc_recv_increments(void) {
    uint64_t s1 = reset();
    (void)s1;
    uint64_t s2 = inc_recv(s1);
    (void)s2;
    t27_assert((get_recv(s2) == 1), "inc works");
}

void test_both_counters(void) {
    uint64_t s1 = reset();
    (void)s1;
    uint64_t s2 = inc_sent(s1);
    (void)s2;
    uint64_t s3 = inc_recv(s2);
    (void)s3;
    t27_assert((get_sent(s3) == 1), "sent");
    t27_assert((get_recv(s3) == 1), "recv");
}

void test_reset_clears(void) {
    uint64_t s1 = reset();
    (void)s1;
    uint64_t s2 = inc_sent(s1);
    (void)s2;
    uint64_t s3 = reset();
    (void)s3;
    t27_assert((get_sent(s3) == 0), "clears");
}


/* -------------------------------------------------------
   Test runner (compile with -DT27_TEST_MAIN to execute)
   ------------------------------------------------------- */

#ifdef T27_TEST_MAIN
#include <stdio.h>
int main(void) {
    test_inc_sent_increments();
    test_inc_recv_increments();
    test_both_counters();
    test_reset_clears();
    printf("All %d tests passed.\n", 4);
    return 0;
}
#endif /* T27_TEST_MAIN */

#endif /* LINKSTATISTICS_H */
