/* ============================================================================
   Generated from t27 spec: ByteUtils
   DO NOT EDIT - generated by t27c gen-c
   phi^2 + 1/phi^2 = 3 | TRINITY
   ============================================================================ */

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <assert.h>

#ifndef BYTEUTILS_H
#define BYTEUTILS_H

/* -------------------------------------------------------
   Function prototypes
   ------------------------------------------------------- */

uint8_t get_bit(uint8_t byte, size_t i);
uint8_t low_nibble(uint8_t byte);
uint8_t high_nibble(uint8_t byte);
uint8_t combine_nibbles(uint8_t high, uint8_t low);
uint8_t swap_nibbles(uint8_t byte);

/* -------------------------------------------------------
   Function implementations
   ------------------------------------------------------- */

uint8_t get_bit(uint8_t byte, size_t i) {
    if ((i == 0)) {
        return (byte & 1);
    } else if ((i == 1)) {
        return ((byte >> 1) & 1);
    } else if ((i == 2)) {
        return ((byte >> 2) & 1);
    } else if ((i == 3)) {
        return ((byte >> 3) & 1);
    } else if ((i == 4)) {
        return ((byte >> 4) & 1);
    } else if ((i == 5)) {
        return ((byte >> 5) & 1);
    } else if ((i == 6)) {
        return ((byte >> 6) & 1);
    } else {
        return ((byte >> 7) & 1);
    }
}

uint8_t low_nibble(uint8_t byte) {
    return (byte & 0x0F);
}

uint8_t high_nibble(uint8_t byte) {
    return ((byte >> 4) & 0x0F);
}

uint8_t combine_nibbles(uint8_t high, uint8_t low) {
    return (((high & 0x0F) << 4) | (low & 0x0F));
}

uint8_t swap_nibbles(uint8_t byte) {
    return (((byte & 0x0F) << 4) | ((byte >> 4) & 0x0F));
}

/* -------------------------------------------------------
   Tests
   ------------------------------------------------------- */

void test_get_bit_lsb(void) {
    bit = get_bit(0x01, 0);
    assert((bit == 1), "LSB is 1");
}

void test_get_bit_msb(void) {
    bit = get_bit(0x80, 7);
    assert((bit == 1), "MSB is 1");
}

void test_low_nibble_test(void) {
    nib = low_nibble(0xAB);
    assert((nib == 0x0B), "low nibble");
}

void test_high_nibble_test(void) {
    nib = high_nibble(0xAB);
    assert((nib == 0x0A), "high nibble");
}

void test_combine_nibbles_test(void) {
    byte = combine_nibbles(0x0A, 0x0B);
    assert((byte == 0xAB), "combine");
}

void test_swap_nibbles_test(void) {
    result = swap_nibbles(0xAB);
    assert((result == 0xBA), "swap");
}


#endif /* BYTEUTILS_H */
