/* ============================================================================
   Generated from t27 spec: SwarmCoordinator
   DO NOT EDIT - generated by t27c gen-c
   phi^2 + 1/phi^2 = 3 | TRINITY
   ============================================================================ */

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <assert.h>

#ifndef SWARMCOORDINATOR_H
#define SWARMCOORDINATOR_H

/* -------------------------------------------------------
   Constants
   ------------------------------------------------------- */

#define MAX_NODES 8
#define QUORUM_THRESHOLD 5
#define PROPOSAL_TIMEOUT 10000
#define VOTE_YES 1
#define VOTE_NO 0
#define VOTE_ABSTAIN 2

/* -------------------------------------------------------
   Function prototypes
   ------------------------------------------------------- */

uint32_t create_proposal(uint32_t proposal_id, uint32_t node_id, uint32_t value, uint32_t timestamp);
uint32_t get_proposal_id(uint32_t proposal);
uint32_t get_proposal_node(uint32_t proposal);
uint32_t get_proposal_value(uint32_t proposal);
uint32_t get_proposal_timestamp(uint32_t proposal);
uint32_t create_vote(uint32_t node_id, uint32_t vote, uint32_t proposal_id, uint32_t timestamp);
uint32_t get_vote_node(uint32_t vote);
uint32_t get_vote_value(uint32_t vote);
uint32_t get_vote_proposal_id(uint32_t vote);
uint32_t get_vote_timestamp(uint32_t vote);
uint64_t create_vote_array(uint32_t v0, uint32_t v1, uint32_t v2, uint32_t v3, uint32_t v4, uint32_t v5, uint32_t v6, uint32_t v7);
uint32_t get_vote(uint64_t array, uint32_t index);
bool has_quorum(uint32_t yes_count, uint32_t no_count, uint32_t abstain_count);
bool proposal_passes(uint32_t yes_count, uint32_t no_count);
uint32_t calculate_consensus_value(uint64_t vote_array, uint32_t proposal_id);
uint32_t cooperative_decision(uint32_t neighbor_values, uint32_t my_value, uint32_t weight_neighbors);

/* -------------------------------------------------------
   Function implementations
   ------------------------------------------------------- */

uint32_t create_proposal(uint32_t proposal_id, uint32_t node_id, uint32_t value, uint32_t timestamp) {
    return (((((proposal_id & 0xFF) << 24) | ((node_id & 0xFF) << 16)) | ((value & 0xFF) << 8)) | (timestamp & 0xFF));
}

uint32_t get_proposal_id(uint32_t proposal) {
    return ((proposal >> 24) & 0xFF);
}

uint32_t get_proposal_node(uint32_t proposal) {
    return ((proposal >> 16) & 0xFF);
}

uint32_t get_proposal_value(uint32_t proposal) {
    return ((proposal >> 8) & 0xFF);
}

uint32_t get_proposal_timestamp(uint32_t proposal) {
    return (proposal & 0xFF);
}

uint32_t create_vote(uint32_t node_id, uint32_t vote, uint32_t proposal_id, uint32_t timestamp) {
    return (((((node_id & 0xFF) << 24) | ((vote & 0x3) << 22)) | ((proposal_id & 0xFF) << 14)) | (timestamp & 0x3FFF));
}

uint32_t get_vote_node(uint32_t vote) {
    return ((vote >> 24) & 0xFF);
}

uint32_t get_vote_value(uint32_t vote) {
    return ((vote >> 22) & 0x3);
}

uint32_t get_vote_proposal_id(uint32_t vote) {
    return ((vote >> 14) & 0xFF);
}

uint32_t get_vote_timestamp(uint32_t vote) {
    return (vote & 0x3FFF);
}

uint64_t create_vote_array(uint32_t v0, uint32_t v1, uint32_t v2, uint32_t v3, uint32_t v4, uint32_t v5, uint32_t v6, uint32_t v7) {
    return ((((((((((uint64_t)(v0)) << 56) | (((uint64_t)(v1)) << 48)) | (((uint64_t)(v2)) << 40)) | (((uint64_t)(v3)) << 32)) | (((uint64_t)(v4)) << 24)) | (((uint64_t)(v5)) << 16)) | (((uint64_t)(v6)) << 8)) | ((uint64_t)(v7)));
}

uint32_t get_vote(uint64_t array, uint32_t index) {
    if ((index == 0)) {
        return ((uint32_t)(((array >> 56) & 0xFFFFFFFF)));
    }
    if ((index == 1)) {
        return ((uint32_t)(((array >> 48) & 0xFFFFFFFF)));
    }
    if ((index == 2)) {
        return ((uint32_t)(((array >> 40) & 0xFFFFFFFF)));
    }
    if ((index == 3)) {
        return ((uint32_t)(((array >> 32) & 0xFFFFFFFF)));
    }
    if ((index == 4)) {
        return ((uint32_t)(((array >> 24) & 0xFFFFFFFF)));
    }
    if ((index == 5)) {
        return ((uint32_t)(((array >> 16) & 0xFFFFFFFF)));
    }
    if ((index == 6)) {
        return ((uint32_t)(((array >> 8) & 0xFFFFFFFF)));
    }
    return ((uint32_t)((array & 0xFFFFFFFF)));
}

bool has_quorum(uint32_t yes_count, uint32_t no_count, uint32_t abstain_count) {
    return (total_voting >= QUORUM_THRESHOLD);
}

bool proposal_passes(uint32_t yes_count, uint32_t no_count) {
    return (yes_count > no_count);
}

uint32_t calculate_consensus_value(uint64_t vote_array, uint32_t proposal_id) {
    if ((get_vote_proposal_id(get_vote(vote_array, 0)) == proposal_id)) {
        sum = (sum + get_proposal_value(get_vote(vote_array, 0)));
        count = (count + 1);
    }
    if ((get_vote_proposal_id(get_vote(vote_array, 1)) == proposal_id)) {
        sum = (sum + get_proposal_value(get_vote(vote_array, 1)));
        count = (count + 1);
    }
    if ((get_vote_proposal_id(get_vote(vote_array, 2)) == proposal_id)) {
        sum = (sum + get_proposal_value(get_vote(vote_array, 2)));
        count = (count + 1);
    }
    if ((get_vote_proposal_id(get_vote(vote_array, 3)) == proposal_id)) {
        sum = (sum + get_proposal_value(get_vote(vote_array, 3)));
        count = (count + 1);
    }
    if ((get_vote_proposal_id(get_vote(vote_array, 4)) == proposal_id)) {
        sum = (sum + get_proposal_value(get_vote(vote_array, 4)));
        count = (count + 1);
    }
    if ((get_vote_proposal_id(get_vote(vote_array, 5)) == proposal_id)) {
        sum = (sum + get_proposal_value(get_vote(vote_array, 5)));
        count = (count + 1);
    }
    if ((get_vote_proposal_id(get_vote(vote_array, 6)) == proposal_id)) {
        sum = (sum + get_proposal_value(get_vote(vote_array, 6)));
        count = (count + 1);
    }
    if ((get_vote_proposal_id(get_vote(vote_array, 7)) == proposal_id)) {
        sum = (sum + get_proposal_value(get_vote(vote_array, 7)));
        count = (count + 1);
    }
    if ((count == 0)) {
        return 0;
    }
    return (sum / count);
}

uint32_t cooperative_decision(uint32_t neighbor_values, uint32_t my_value, uint32_t weight_neighbors) {
    return (weighted_neighbors + weighted_self);
}

/* -------------------------------------------------------
   Tests
   ------------------------------------------------------- */

void test_create_proposal_basic(void) {
    proposal = create_proposal(5, 10, 100, 1000);
    assert((get_proposal_id(proposal) == 5), "proposal id");
    assert((get_proposal_node(proposal) == 10), "node id");
    assert((get_proposal_value(proposal) == 100), "value");
    assert((get_proposal_timestamp(proposal) == 1000), "timestamp");
}

void test_create_vote_yes(void) {
    vote = create_vote(5, VOTE_YES, 10, 1000);
    assert((get_vote_node(vote) == 5), "node");
    assert((get_vote_value(vote) == VOTE_YES), "yes vote");
    assert((get_vote_proposal_id(vote) == 10), "proposal id");
}

void test_create_vote_no(void) {
    vote = create_vote(5, VOTE_NO, 10, 1000);
    assert((get_vote_value(vote) == VOTE_NO), "no vote");
}

void test_create_vote_abstain(void) {
    vote = create_vote(5, VOTE_ABSTAIN, 10, 1000);
    assert((get_vote_value(vote) == VOTE_ABSTAIN), "abstain vote");
}

void test_count_votes_unanimous_yes(void) {
    vote_array = create_vote_array(create_vote(1, VOTE_YES, 10, 1000), create_vote(2, VOTE_YES, 10, 1000), create_vote(3, VOTE_YES, 10, 1000), create_vote(4, VOTE_YES, 10, 1000), create_vote(5, VOTE_YES, 10, 1000), create_vote(6, VOTE_YES, 10, 1000), create_vote(7, VOTE_YES, 10, 1000), create_vote(8, VOTE_YES, 10, 1000));
    let(yes, no, abstain) = count_votes(vote_array, 10);
    assert((yes == 8), "8 yes votes");
    assert((no == 0), "0 no votes");
    assert((abstain == 0), "0 abstain");
}

void test_count_votes_mixed(void) {
    vote_array = create_vote_array(create_vote(1, VOTE_YES, 10, 1000), create_vote(2, VOTE_NO, 10, 1000), create_vote(3, VOTE_YES, 10, 1000), create_vote(4, VOTE_ABSTAIN, 10, 1000), create_vote(5, VOTE_YES, 10, 1000), create_vote(6, VOTE_NO, 10, 1000), create_vote(7, VOTE_YES, 10, 1000), create_vote(8, VOTE_ABSTAIN, 10, 1000));
    let(yes, no, abstain) = count_votes(vote_array, 10);
    assert((yes == 4), "4 yes votes");
    assert((no == 2), "2 no votes");
    assert((abstain == 2), "2 abstain");
}

void test_has_quorum_true(void) {
    assert((has_quorum(yes, no, abstain) == true), "quorum reached");
}

void test_has_quorum_false(void) {
    assert((has_quorum(yes, no, abstain) == false), "no quorum");
}

void test_proposal_passes_yes(void) {
    assert((proposal_passes(yes, no) == true), "proposal passes");
}

void test_proposal_passes_no(void) {
    assert((proposal_passes(yes, no) == false), "proposal fails");
}

void test_proposal_passes_tie(void) {
    assert((proposal_passes(yes, no) == false), "proposal fails on tie");
}

void test_calculate_consensus_value_average(void) {
    vote_array = create_vote_array(create_proposal(1, 10, 100, 1000), create_proposal(2, 10, 200, 1000), create_proposal(3, 10, 150, 1000), create_proposal(4, 10, 250, 1000), create_proposal(5, 10, 0, 0), create_proposal(6, 10, 0, 0), create_proposal(7, 10, 0, 0), create_proposal(8, 10, 0, 0));
    assert((consensus == 175), "average of 100,200,150,250 = 175");
}

void test_calculate_consensus_value_empty(void) {
    vote_array = create_vote_array(create_proposal(1, 99, 100, 1000), create_proposal(2, 99, 200, 1000), create_proposal(3, 10, 0, 0), create_proposal(4, 10, 0, 0), create_proposal(5, 10, 0, 0), create_proposal(6, 10, 0, 0), create_proposal(7, 10, 0, 0), create_proposal(8, 10, 0, 0));
    assert((consensus == 0), "no votes for proposal");
}

void test_cooperative_decision_equal_weight(void) {
    assert((result == 70), "equal weighted average");
}

void test_cooperative_decision_neighbor_heavy(void) {
    assert((result == 90), "neighbor-weighted");
}

void test_cooperative_decision_self_heavy(void) {
    assert((result == 90), "self-weighted");
}


#endif /* SWARMCOORDINATOR_H */
