/* ============================================================================
   Generated from t27 spec: performance_profiler
   DO NOT EDIT - generated by t27c gen-c
   phi^2 + 1/phi^2 = 3 | TRINITY
   ============================================================================ */

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <assert.h>
#define t27_assert(c, m) do { if (!(c)) { __builtin_trap(); } } while (0)
#define assert_eq(a, b) do { if ((a) != (b)) { __builtin_trap(); } } while (0)

#ifndef PERFORMANCE_PROFILER_H
#define PERFORMANCE_PROFILER_H

/* -------------------------------------------------------
   Constants
   ------------------------------------------------------- */

#define MAX_SAMPLES 64
#define MAX_FUNCTIONS 32
#define PROFILING_INTERVAL_MS 100
#define OVERHEAD_THRESHOLD 5

/* -------------------------------------------------------
   Array value types ([T; N] lowers to a by-value struct)
   ------------------------------------------------------- */

typedef struct { uint32_t v[64]; } t27_arr_uint32_t_64;
typedef struct { uint32_t v[32]; } t27_arr_uint32_t_32;

/* -------------------------------------------------------
   Function prototypes
   ------------------------------------------------------- */

uint32_t create_perf_sample(uint32_t func_id, uint32_t cpu, uint32_t memory, uint32_t timestamp);
uint32_t get_sample_function_id(uint32_t sample);
uint32_t get_sample_cpu(uint32_t sample);
uint32_t get_sample_memory(uint32_t sample);
uint32_t get_sample_timestamp(uint32_t sample);
uint32_t create_function_profile(uint32_t func_id, uint32_t calls, uint32_t total_cpu, uint32_t total_mem);
uint32_t get_profile_function_id(uint32_t profile);
uint32_t get_profile_call_count(uint32_t profile);
uint32_t get_profile_total_cpu(uint32_t profile);
uint32_t create_hotspot(uint32_t func_id, uint32_t score, uint32_t rank, uint32_t impact);
uint32_t get_hotspot_function_id(uint32_t hotspot);
uint32_t get_hotspot_score(uint32_t hotspot);
uint32_t get_hotspot_rank(uint32_t hotspot);
uint32_t get_hotspot_impact(uint32_t hotspot);
uint32_t calculate_average_cpu(t27_arr_uint32_t_64 samples, uint32_t sample_count, uint32_t func_id);
uint32_t calculate_average_memory(t27_arr_uint32_t_64 samples, uint32_t sample_count, uint32_t func_id);
uint32_t identify_hotspots(t27_arr_uint32_t_32 profiles, uint32_t profile_count);
uint32_t calculate_profiling_overhead(uint32_t base_runtime, uint32_t profiled_runtime);
uint32_t is_overhead_acceptable(uint32_t overhead_percentage);
uint32_t create_allocation(uint32_t alloc_id, uint32_t size, uint32_t lifetime, uint32_t pool);
uint32_t get_allocation_id(uint32_t alloc);
uint32_t get_allocation_size(uint32_t alloc);
uint32_t get_allocation_lifetime(uint32_t alloc);
uint32_t get_allocation_pool(uint32_t alloc);
uint32_t track_allocation(t27_arr_uint32_t_64 allocations, uint32_t alloc_id, uint32_t size, uint32_t pool);
uint32_t calculate_total_memory(t27_arr_uint32_t_64 allocations, uint32_t sample_count);
uint32_t detect_memory_leak(t27_arr_uint32_t_64 allocations, uint32_t current_count, uint32_t previous_count);
uint32_t create_call_stack_entry(uint32_t depth, uint32_t func_id, uint32_t parent_id, uint32_t cpu_contrib);
uint32_t get_stack_depth(uint32_t entry);
uint32_t get_stack_function_id(uint32_t entry);
uint32_t get_stack_parent_id(uint32_t entry);
uint32_t get_stack_cpu_contribution(uint32_t entry);
uint32_t analyze_call_tree(t27_arr_uint32_t_64 call_stack, uint32_t stack_size);
uint32_t create_performance_report(uint32_t total_cpu, uint32_t total_mem, uint32_t hotspots, uint32_t overhead);
uint32_t get_report_total_cpu(uint32_t report);
uint32_t get_report_total_memory(uint32_t report);
uint32_t get_report_hotspot_count(uint32_t report);
uint32_t get_report_overhead(uint32_t report);
uint32_t generate_recommendations(uint32_t report, uint32_t hotspot);
uint32_t calculate_improvement_opportunity(uint32_t current_performance, uint32_t target_performance);

/* -------------------------------------------------------
   Function implementations
   ------------------------------------------------------- */

uint32_t create_perf_sample(uint32_t func_id, uint32_t cpu, uint32_t memory, uint32_t timestamp) {
    return (((((func_id & 0xFF) << 24) | ((cpu & 0xFF) << 16)) | ((memory & 0xFF) << 8)) | (timestamp & 0xFF));
}

uint32_t get_sample_function_id(uint32_t sample) {
    return ((sample >> 24) & 0xFF);
}

uint32_t get_sample_cpu(uint32_t sample) {
    return ((sample >> 16) & 0xFF);
}

uint32_t get_sample_memory(uint32_t sample) {
    return ((sample >> 8) & 0xFF);
}

uint32_t get_sample_timestamp(uint32_t sample) {
    return (sample & 0xFF);
}

uint32_t create_function_profile(uint32_t func_id, uint32_t calls, uint32_t total_cpu, uint32_t total_mem) {
    return ((((func_id & 0xFF) << 24) | ((calls & 0xFFFF) << 8)) | (total_cpu & 0xFF));
}

uint32_t get_profile_function_id(uint32_t profile) {
    return ((profile >> 24) & 0xFF);
}

uint32_t get_profile_call_count(uint32_t profile) {
    return ((profile >> 8) & 0xFFFF);
}

uint32_t get_profile_total_cpu(uint32_t profile) {
    return (profile & 0xFF);
}

uint32_t create_hotspot(uint32_t func_id, uint32_t score, uint32_t rank, uint32_t impact) {
    return (((((func_id & 0xFF) << 24) | ((score & 0xFF) << 16)) | ((rank & 0xFF) << 8)) | (impact & 0xFF));
}

uint32_t get_hotspot_function_id(uint32_t hotspot) {
    return ((hotspot >> 24) & 0xFF);
}

uint32_t get_hotspot_score(uint32_t hotspot) {
    return ((hotspot >> 16) & 0xFF);
}

uint32_t get_hotspot_rank(uint32_t hotspot) {
    return ((hotspot >> 8) & 0xFF);
}

uint32_t get_hotspot_impact(uint32_t hotspot) {
    return (hotspot & 0xFF);
}

uint32_t calculate_average_cpu(t27_arr_uint32_t_64 samples, uint32_t sample_count, uint32_t func_id) {
    uint32_t total_cpu = 0;
    uint32_t matching_samples = 0;
    uint32_t i = 0;
    while ((i < sample_count)) {
        if ((get_sample_function_id(samples.v[i]) == func_id)) {
            total_cpu = (total_cpu + get_sample_cpu(samples.v[i]));
            matching_samples = (matching_samples + 1);
        }
        i = (i + 1);
    }
    if ((matching_samples > 0)) {
        return (total_cpu / matching_samples);
    } else {
        return 0;
    }
}

uint32_t calculate_average_memory(t27_arr_uint32_t_64 samples, uint32_t sample_count, uint32_t func_id) {
    uint32_t total_memory = 0;
    uint32_t matching_samples = 0;
    uint32_t i = 0;
    while ((i < sample_count)) {
        if ((get_sample_function_id(samples.v[i]) == func_id)) {
            total_memory = (total_memory + get_sample_memory(samples.v[i]));
            matching_samples = (matching_samples + 1);
        }
        i = (i + 1);
    }
    if ((matching_samples > 0)) {
        return (total_memory / matching_samples);
    } else {
        return 0;
    }
}

uint32_t identify_hotspots(t27_arr_uint32_t_32 profiles, uint32_t profile_count) {
    uint32_t max_calls = 0;
    uint32_t max_cpu = 0;
    uint32_t hotspot_func = 0;
    uint32_t i = 0;
    while ((i < profile_count)) {
        uint32_t calls = get_profile_call_count(profiles.v[i]);
        uint32_t cpu = get_profile_total_cpu(profiles.v[i]);
        if (((calls > max_calls) || ((calls == max_calls) && (cpu > max_cpu)))) {
            max_calls = calls;
            max_cpu = cpu;
            hotspot_func = get_profile_function_id(profiles.v[i]);
        }
        i = (i + 1);
    }
    uint32_t score = ((max_calls * 10) + max_cpu);
    if ((score > 255)) {
        score = 255;
    }
    return create_hotspot(hotspot_func, score, 1, score);
}

uint32_t calculate_profiling_overhead(uint32_t base_runtime, uint32_t profiled_runtime) {
    if ((base_runtime == 0)) {
        return 0;
    }
    uint32_t overhead = (profiled_runtime - base_runtime);
    uint32_t overhead_percentage = ((overhead * 100) / base_runtime);
    return overhead_percentage;
}

uint32_t is_overhead_acceptable(uint32_t overhead_percentage) {
    if ((overhead_percentage <= OVERHEAD_THRESHOLD)) {
        return 1;
    } else {
        return 0;
    }
}

uint32_t create_allocation(uint32_t alloc_id, uint32_t size, uint32_t lifetime, uint32_t pool) {
    return (((((alloc_id & 0xFF) << 24) | ((size & 0xFF) << 16)) | ((lifetime & 0xFF) << 8)) | (pool & 0xFF));
}

uint32_t get_allocation_id(uint32_t alloc) {
    return ((alloc >> 24) & 0xFF);
}

uint32_t get_allocation_size(uint32_t alloc) {
    return ((alloc >> 16) & 0xFF);
}

uint32_t get_allocation_lifetime(uint32_t alloc) {
    return ((alloc >> 8) & 0xFF);
}

uint32_t get_allocation_pool(uint32_t alloc) {
    return (alloc & 0xFF);
}

uint32_t track_allocation(t27_arr_uint32_t_64 allocations, uint32_t alloc_id, uint32_t size, uint32_t pool) {
    uint32_t i = 0;
    while ((i < MAX_SAMPLES)) {
        if ((get_allocation_id(allocations.v[i]) == 0)) {
            allocations.v[i] = create_allocation(alloc_id, size, 255, pool);
            return 1;
        }
        i = (i + 1);
    }
    return 0;
}

uint32_t calculate_total_memory(t27_arr_uint32_t_64 allocations, uint32_t sample_count) {
    uint32_t total_memory = 0;
    uint32_t i = 0;
    while ((i < sample_count)) {
        uint32_t size = get_allocation_size(allocations.v[i]);
        total_memory = (total_memory + size);
        i = (i + 1);
    }
    return total_memory;
}

uint32_t detect_memory_leak(t27_arr_uint32_t_64 allocations, uint32_t current_count, uint32_t previous_count) {
    if ((current_count > previous_count)) {
        uint32_t growth = (current_count - previous_count);
        if ((growth > 5)) {
            return 1;
        }
    }
    return 0;
}

uint32_t create_call_stack_entry(uint32_t depth, uint32_t func_id, uint32_t parent_id, uint32_t cpu_contrib) {
    return (((((depth & 0xFF) << 24) | ((func_id & 0xFF) << 16)) | ((parent_id & 0xFF) << 8)) | (cpu_contrib & 0xFF));
}

uint32_t get_stack_depth(uint32_t entry) {
    return ((entry >> 24) & 0xFF);
}

uint32_t get_stack_function_id(uint32_t entry) {
    return ((entry >> 16) & 0xFF);
}

uint32_t get_stack_parent_id(uint32_t entry) {
    return ((entry >> 8) & 0xFF);
}

uint32_t get_stack_cpu_contribution(uint32_t entry) {
    return (entry & 0xFF);
}

uint32_t analyze_call_tree(t27_arr_uint32_t_64 call_stack, uint32_t stack_size) {
    uint32_t max_depth = 0;
    uint32_t total_cpu = 0;
    uint32_t i = 0;
    while ((i < stack_size)) {
        uint32_t depth = get_stack_depth(call_stack.v[i]);
        uint32_t cpu = get_stack_cpu_contribution(call_stack.v[i]);
        if ((depth > max_depth)) {
            max_depth = depth;
        }
        total_cpu = (total_cpu + cpu);
        i = (i + 1);
    }
    uint32_t avg_cpu = 0;
    if ((max_depth > 0)) {
        avg_cpu = (total_cpu / max_depth);
    }
    return ((((max_depth & 0xFF) << 24) | ((total_cpu & 0xFF) << 16)) | ((avg_cpu & 0xFF) << 8));
}

uint32_t create_performance_report(uint32_t total_cpu, uint32_t total_mem, uint32_t hotspots, uint32_t overhead) {
    return (((((total_cpu & 0xFF) << 24) | ((total_mem & 0xFF) << 16)) | ((hotspots & 0xFF) << 8)) | (overhead & 0xFF));
}

uint32_t get_report_total_cpu(uint32_t report) {
    return ((report >> 24) & 0xFF);
}

uint32_t get_report_total_memory(uint32_t report) {
    return ((report >> 16) & 0xFF);
}

uint32_t get_report_hotspot_count(uint32_t report) {
    return ((report >> 8) & 0xFF);
}

uint32_t get_report_overhead(uint32_t report) {
    return (report & 0xFF);
}

uint32_t generate_recommendations(uint32_t report, uint32_t hotspot) {
    uint32_t total_cpu = get_report_total_cpu(report);
    uint32_t hotspot_score = get_hotspot_score(hotspot);
    uint32_t overhead = get_report_overhead(report);
    uint32_t rec_optimize_cpu = 0;
    uint32_t rec_optimize_memory = 0;
    uint32_t rec_reduce_overhead = 0;
    uint32_t rec_parallelize = 0;
    if ((total_cpu > 80)) {
        rec_optimize_cpu = 1;
    }
    if ((hotspot_score > 100)) {
        rec_parallelize = 1;
    }
    if ((overhead > OVERHEAD_THRESHOLD)) {
        rec_reduce_overhead = 1;
    }
    return (((((rec_optimize_cpu & 0x1) << 3) | ((rec_optimize_memory & 0x1) << 2)) | ((rec_reduce_overhead & 0x1) << 1)) | (rec_parallelize & 0x1));
}

uint32_t calculate_improvement_opportunity(uint32_t current_performance, uint32_t target_performance) {
    if ((current_performance >= target_performance)) {
        return 0;
    }
    uint32_t gap = (target_performance - current_performance);
    uint32_t opportunity = ((gap * 100) / target_performance);
    return opportunity;
}

/* -------------------------------------------------------
   Tests
   ------------------------------------------------------- */

void test_perf_sample_roundtrip(void) {
    uint64_t smp = create_perf_sample(6, 45, 30, 99);
    (void)smp;
    t27_assert((get_sample_function_id(smp) == 6), "function id");
    t27_assert((get_sample_cpu(smp) == 45), "cpu");
    t27_assert((get_sample_memory(smp) == 30), "memory");
    t27_assert((get_sample_timestamp(smp) == 99), "timestamp");
}

void test_hotspot_roundtrip(void) {
    uint64_t h = create_hotspot(6, 90, 1, 77);
    (void)h;
    t27_assert((get_hotspot_function_id(h) == 6), "function id");
    t27_assert((get_hotspot_score(h) == 90), "score");
    t27_assert((get_hotspot_rank(h) == 1), "rank");
    t27_assert((get_hotspot_impact(h) == 77), "impact");
}


/* -------------------------------------------------------
   Test runner (compile with -DT27_TEST_MAIN to execute)
   ------------------------------------------------------- */

#ifdef T27_TEST_MAIN
#include <stdio.h>
int main(void) {
    test_perf_sample_roundtrip();
    test_hotspot_roundtrip();
    printf("All %d tests passed.\n", 2);
    return 0;
}
#endif /* T27_TEST_MAIN */

#endif /* PERFORMANCE_PROFILER_H */
