// Generated from .t27 spec
// DO NOT EDIT — generated by t27c
// NOT LOWERED BY THIS BACKEND: 8 test(s), 0 invariant(s).
// This backend emits declarations only. The spec's checks live in
// the Zig and Verilog outputs; do not read this file as verified.

pub const MANT_ONE: u32 = 512;

pub const MANT_BITS: u32 = 9;

pub const ALIGN_CAP: u32 = 22;

pub fn hi_bit(x: u32) -> u32 {
    if (x >= 1073741824) {
        return 30;
    }
    if (x >= 536870912) {
        return 29;
    }
    if (x >= 268435456) {
        return 28;
    }
    if (x >= 134217728) {
        return 27;
    }
    if (x >= 67108864) {
        return 26;
    }
    if (x >= 33554432) {
        return 25;
    }
    if (x >= 16777216) {
        return 24;
    }
    if (x >= 8388608) {
        return 23;
    }
    if (x >= 4194304) {
        return 22;
    }
    if (x >= 2097152) {
        return 21;
    }
    if (x >= 1048576) {
        return 20;
    }
    if (x >= 524288) {
        return 19;
    }
    if (x >= 262144) {
        return 18;
    }
    if (x >= 131072) {
        return 17;
    }
    if (x >= 65536) {
        return 16;
    }
    if (x >= 32768) {
        return 15;
    }
    if (x >= 16384) {
        return 14;
    }
    if (x >= 8192) {
        return 13;
    }
    if (x >= 4096) {
        return 12;
    }
    if (x >= 2048) {
        return 11;
    }
    if (x >= 1024) {
        return 10;
    }
    if (x >= 512) {
        return 9;
    }
    if (x >= 256) {
        return 8;
    }
    if (x >= 128) {
        return 7;
    }
    if (x >= 64) {
        return 6;
    }
    if (x >= 32) {
        return 5;
    }
    if (x >= 16) {
        return 4;
    }
    if (x >= 8) {
        return 3;
    }
    if (x >= 4) {
        return 2;
    }
    if (x >= 2) {
        return 1;
    }
    return 0;
}

pub fn norm_sig_p(v: u32, hb: u32, mant_bits: u32) -> u32 {
    if (hb >= mant_bits) {
        return (v >> (hb - mant_bits));
    } else {
        return (v << (mant_bits - hb));
    }
}

pub fn gft_sub_offset_p(
    offset_a: u32,
    offset_b: u32,
    ma: u32,
    mb: u32,
    mant_one: u32,
    mant_bits: u32,
) -> u32 {
    let d: u32 = (offset_a - offset_b);
    if (d >= ALIGN_CAP) {
        if (ma >= 1) {
            return offset_a;
        } else {
            if (offset_a >= 1) {
                return (offset_a - 1);
            } else {
                return 0;
            }
        }
    } else {
        let v: u32 = (((mant_one + ma) << d) - (mant_one + mb));
        if (v == 0) {
            return 0;
        } else {
            let hb: u32 = hi_bit(v);
            if ((offset_b + hb) < mant_bits) {
                return 0;
            } else {
                return ((offset_b + hb) - mant_bits);
            }
        }
    }
}

pub fn gft_sub_mant_p(
    offset_a: u32,
    offset_b: u32,
    ma: u32,
    mb: u32,
    mant_one: u32,
    mant_bits: u32,
) -> u32 {
    let d: u32 = (offset_a - offset_b);
    if (d >= ALIGN_CAP) {
        if (ma >= 1) {
            return (ma - 1);
        } else {
            return (mant_one - 1);
        }
    } else {
        let v: u32 = (((mant_one + ma) << d) - (mant_one + mb));
        if (v == 0) {
            return 0;
        } else {
            let hb: u32 = hi_bit(v);
            if ((offset_b + hb) < mant_bits) {
                return 0;
            } else {
                return (norm_sig_p(v, hb, mant_bits) - mant_one);
            }
        }
    }
}

pub fn norm_sig(v: u32, hb: u32) -> u32 {
    return norm_sig_p(v, hb, MANT_BITS);
}

pub fn gft_sub_offset(offset_a: u32, offset_b: u32, ma: u32, mb: u32) -> u32 {
    return gft_sub_offset_p(offset_a, offset_b, ma, mb, MANT_ONE, MANT_BITS);
}

pub fn gft_sub_mant(offset_a: u32, offset_b: u32, ma: u32, mb: u32) -> u32 {
    return gft_sub_mant_p(offset_a, offset_b, ma, mb, MANT_ONE, MANT_BITS);
}

pub fn gft_sub_offset_c(oa: u32, ob: u32, ma: u32, mb: u32) -> u32 {
    if (oa > ob) {
        return gft_sub_offset(oa, ob, ma, mb);
    } else {
        if (ob > oa) {
            return gft_sub_offset(ob, oa, mb, ma);
        } else {
            if (ma >= mb) {
                return gft_sub_offset(oa, ob, ma, mb);
            } else {
                return gft_sub_offset(ob, oa, mb, ma);
            }
        }
    }
}

pub fn gft_sub_mant_c(oa: u32, ob: u32, ma: u32, mb: u32) -> u32 {
    if (oa > ob) {
        return gft_sub_mant(oa, ob, ma, mb);
    } else {
        if (ob > oa) {
            return gft_sub_mant(ob, oa, mb, ma);
        } else {
            if (ma >= mb) {
                return gft_sub_mant(oa, ob, ma, mb);
            } else {
                return gft_sub_mant(ob, oa, mb, ma);
            }
        }
    }
}

pub fn verify_gft_sub(
    oa: u32,
    ob: u32,
    ma: u32,
    mb: u32,
    claimed_offset: u32,
    claimed_mant: u32,
) -> bool {
    if (gft_sub_offset_c(oa, ob, ma, mb) == claimed_offset) {
        return (gft_sub_mant_c(oa, ob, ma, mb) == claimed_mant);
    } else {
        return (oa != oa);
    }
}

pub fn gft_sub_offset_c_p(
    oa: u32,
    ob: u32,
    ma: u32,
    mb: u32,
    mant_one: u32,
    mant_bits: u32,
) -> u32 {
    if (oa > ob) {
        return gft_sub_offset_p(oa, ob, ma, mb, mant_one, mant_bits);
    } else {
        if (ob > oa) {
            return gft_sub_offset_p(ob, oa, mb, ma, mant_one, mant_bits);
        } else {
            if (ma >= mb) {
                return gft_sub_offset_p(oa, ob, ma, mb, mant_one, mant_bits);
            } else {
                return gft_sub_offset_p(ob, oa, mb, ma, mant_one, mant_bits);
            }
        }
    }
}

pub fn gft_sub_mant_c_p(oa: u32, ob: u32, ma: u32, mb: u32, mant_one: u32, mant_bits: u32) -> u32 {
    if (oa > ob) {
        return gft_sub_mant_p(oa, ob, ma, mb, mant_one, mant_bits);
    } else {
        if (ob > oa) {
            return gft_sub_mant_p(ob, oa, mb, ma, mant_one, mant_bits);
        } else {
            if (ma >= mb) {
                return gft_sub_mant_p(oa, ob, ma, mb, mant_one, mant_bits);
            } else {
                return gft_sub_mant_p(ob, oa, mb, ma, mant_one, mant_bits);
            }
        }
    }
}

pub fn verify_gft_sub_p(
    oa: u32,
    ob: u32,
    ma: u32,
    mb: u32,
    claimed_offset: u32,
    claimed_mant: u32,
    mant_one: u32,
    mant_bits: u32,
) -> bool {
    if (gft_sub_offset_c_p(oa, ob, ma, mb, mant_one, mant_bits) == claimed_offset) {
        return (gft_sub_mant_c_p(oa, ob, ma, mb, mant_one, mant_bits) == claimed_mant);
    } else {
        return (oa != oa);
    }
}

pub fn hi_bit_u64(x: u64) -> u32 {
    if ((x >> 63) != 0) {
        return 63;
    }
    if ((x >> 62) != 0) {
        return 62;
    }
    if ((x >> 61) != 0) {
        return 61;
    }
    if ((x >> 60) != 0) {
        return 60;
    }
    if ((x >> 59) != 0) {
        return 59;
    }
    if ((x >> 58) != 0) {
        return 58;
    }
    if ((x >> 57) != 0) {
        return 57;
    }
    if ((x >> 56) != 0) {
        return 56;
    }
    if ((x >> 55) != 0) {
        return 55;
    }
    if ((x >> 54) != 0) {
        return 54;
    }
    if ((x >> 53) != 0) {
        return 53;
    }
    if ((x >> 52) != 0) {
        return 52;
    }
    if ((x >> 51) != 0) {
        return 51;
    }
    if ((x >> 50) != 0) {
        return 50;
    }
    if ((x >> 49) != 0) {
        return 49;
    }
    if ((x >> 48) != 0) {
        return 48;
    }
    if ((x >> 47) != 0) {
        return 47;
    }
    if ((x >> 46) != 0) {
        return 46;
    }
    if ((x >> 45) != 0) {
        return 45;
    }
    if ((x >> 44) != 0) {
        return 44;
    }
    if ((x >> 43) != 0) {
        return 43;
    }
    if ((x >> 42) != 0) {
        return 42;
    }
    if ((x >> 41) != 0) {
        return 41;
    }
    if ((x >> 40) != 0) {
        return 40;
    }
    if ((x >> 39) != 0) {
        return 39;
    }
    if ((x >> 38) != 0) {
        return 38;
    }
    if ((x >> 37) != 0) {
        return 37;
    }
    if ((x >> 36) != 0) {
        return 36;
    }
    if ((x >> 35) != 0) {
        return 35;
    }
    if ((x >> 34) != 0) {
        return 34;
    }
    if ((x >> 33) != 0) {
        return 33;
    }
    if ((x >> 32) != 0) {
        return 32;
    }
    if ((x >> 31) != 0) {
        return 31;
    }
    if ((x >> 30) != 0) {
        return 30;
    }
    if ((x >> 29) != 0) {
        return 29;
    }
    if ((x >> 28) != 0) {
        return 28;
    }
    if ((x >> 27) != 0) {
        return 27;
    }
    if ((x >> 26) != 0) {
        return 26;
    }
    if ((x >> 25) != 0) {
        return 25;
    }
    if ((x >> 24) != 0) {
        return 24;
    }
    if ((x >> 23) != 0) {
        return 23;
    }
    if ((x >> 22) != 0) {
        return 22;
    }
    if ((x >> 21) != 0) {
        return 21;
    }
    if ((x >> 20) != 0) {
        return 20;
    }
    if ((x >> 19) != 0) {
        return 19;
    }
    if ((x >> 18) != 0) {
        return 18;
    }
    if ((x >> 17) != 0) {
        return 17;
    }
    if ((x >> 16) != 0) {
        return 16;
    }
    if ((x >> 15) != 0) {
        return 15;
    }
    if ((x >> 14) != 0) {
        return 14;
    }
    if ((x >> 13) != 0) {
        return 13;
    }
    if ((x >> 12) != 0) {
        return 12;
    }
    if ((x >> 11) != 0) {
        return 11;
    }
    if ((x >> 10) != 0) {
        return 10;
    }
    if ((x >> 9) != 0) {
        return 9;
    }
    if ((x >> 8) != 0) {
        return 8;
    }
    if ((x >> 7) != 0) {
        return 7;
    }
    if ((x >> 6) != 0) {
        return 6;
    }
    if ((x >> 5) != 0) {
        return 5;
    }
    if ((x >> 4) != 0) {
        return 4;
    }
    if ((x >> 3) != 0) {
        return 3;
    }
    if ((x >> 2) != 0) {
        return 2;
    }
    if ((x >> 1) != 0) {
        return 1;
    }
    return 0;
}

pub fn norm_sig_u64(v: u64, hb: u32, mant_bits: u32) -> u64 {
    if (hb >= mant_bits) {
        return (v >> (hb - mant_bits));
    } else {
        return (v << (mant_bits - hb));
    }
}

pub fn gft_sub_offset_u64(
    offset_a: u32,
    offset_b: u32,
    ma: u64,
    mb: u64,
    mant_one: u64,
    mant_bits: u32,
    align_cap: u32,
) -> u32 {
    let d: u32 = (offset_a - offset_b);
    if (d >= align_cap) {
        if (ma >= 1) {
            return offset_a;
        } else {
            if (offset_a >= 1) {
                return (offset_a - 1);
            } else {
                return 0;
            }
        }
    } else {
        let v: u64 = (((mant_one + ma) << d) - (mant_one + mb));
        if (v == 0) {
            return 0;
        } else {
            let hb: u32 = hi_bit_u64(v);
            if ((offset_b + hb) < mant_bits) {
                return 0;
            } else {
                return ((offset_b + hb) - mant_bits);
            }
        }
    }
}

pub fn gft_sub_mant_u64(
    offset_a: u32,
    offset_b: u32,
    ma: u64,
    mb: u64,
    mant_one: u64,
    mant_bits: u32,
    align_cap: u32,
) -> u32 {
    let d: u32 = (offset_a - offset_b);
    if (d >= align_cap) {
        if (ma >= 1) {
            return ((ma - 1) as u32);
        } else {
            return ((mant_one - 1) as u32);
        }
    } else {
        let v: u64 = (((mant_one + ma) << d) - (mant_one + mb));
        if (v == 0) {
            return 0;
        } else {
            let hb: u32 = hi_bit_u64(v);
            if ((offset_b + hb) < mant_bits) {
                return 0;
            } else {
                return ((norm_sig_u64(v, hb, mant_bits) - mant_one) as u32);
            }
        }
    }
}

pub fn gft_sub_offset_c_u64(
    oa: u32,
    ob: u32,
    ma: u64,
    mb: u64,
    mant_one: u64,
    mant_bits: u32,
    align_cap: u32,
) -> u32 {
    if (oa > ob) {
        return gft_sub_offset_u64(oa, ob, ma, mb, mant_one, mant_bits, align_cap);
    } else {
        if (ob > oa) {
            return gft_sub_offset_u64(ob, oa, mb, ma, mant_one, mant_bits, align_cap);
        } else {
            if (ma >= mb) {
                return gft_sub_offset_u64(oa, ob, ma, mb, mant_one, mant_bits, align_cap);
            } else {
                return gft_sub_offset_u64(ob, oa, mb, ma, mant_one, mant_bits, align_cap);
            }
        }
    }
}

pub fn gft_sub_mant_c_u64(
    oa: u32,
    ob: u32,
    ma: u64,
    mb: u64,
    mant_one: u64,
    mant_bits: u32,
    align_cap: u32,
) -> u32 {
    if (oa > ob) {
        return gft_sub_mant_u64(oa, ob, ma, mb, mant_one, mant_bits, align_cap);
    } else {
        if (ob > oa) {
            return gft_sub_mant_u64(ob, oa, mb, ma, mant_one, mant_bits, align_cap);
        } else {
            if (ma >= mb) {
                return gft_sub_mant_u64(oa, ob, ma, mb, mant_one, mant_bits, align_cap);
            } else {
                return gft_sub_mant_u64(ob, oa, mb, ma, mant_one, mant_bits, align_cap);
            }
        }
    }
}

pub fn verify_gft_sub_u64(
    oa: u32,
    ob: u32,
    ma: u64,
    mb: u64,
    claimed_offset: u32,
    claimed_mant: u32,
    mant_one: u64,
    mant_bits: u32,
    align_cap: u32,
) -> bool {
    if (gft_sub_offset_c_u64(oa, ob, ma, mb, mant_one, mant_bits, align_cap) == claimed_offset) {
        return (gft_sub_mant_c_u64(oa, ob, ma, mb, mant_one, mant_bits, align_cap)
            == claimed_mant);
    } else {
        return (oa != oa);
    }
}
