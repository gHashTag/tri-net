// Generated from .t27 spec
// DO NOT EDIT — generated by t27c

pub const MIN_KEY_BITS: u32 = 256;

pub const NONCE_LEN: u32 = 12;

pub const TAG_LEN: u32 = 16;

pub const MAX_REPLAY_WINDOW: u32 = 1024;

pub const SESSION_TIMEOUT_S: u32 = 3600;

pub const MAX_AUTH_FAILS: u8 = 5;

pub fn key_strength_ok(bits: u32) -> bool {
    return (bits >= MIN_KEY_BITS);
}

pub fn nonce_unique(nonce_low: u32, prev_nonce: u32) -> bool {
    return (nonce_low != prev_nonce);
}

pub fn tag_length_ok(len: u32) -> bool {
    return (len == TAG_LEN);
}

pub fn auth_attempts_ok(fails: u8) -> bool {
    return (fails < MAX_AUTH_FAILS);
}

pub fn session_valid(age_s: u32) -> bool {
    return (age_s < SESSION_TIMEOUT_S);
}

pub fn replay_window_ok(seq: u32, last_seen: u32) -> bool {
    if (seq <= last_seen) {
        return false;
    }
    return ((seq - last_seen) < MAX_REPLAY_WINDOW);
}

pub fn key_rotation_due(age_s: u32) -> bool {
    return (age_s >= SESSION_TIMEOUT_S);
}

pub fn constant_time_compare(a: u8, b: u8) -> u8 {
    return (a ^ b);
}

pub fn zeroize_needed(sensitive_data_present: bool) -> bool {
    return sensitive_data_present;
}

