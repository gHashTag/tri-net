// Generated from .t27 spec
// DO NOT EDIT — generated by t27c
// NOT LOWERED BY THIS BACKEND: 14 test(s), 2 invariant(s).
// This backend emits declarations only. The spec's checks live in
// the Zig and Verilog outputs; do not read this file as verified.

pub const MILLI: u32 = 1000;

pub const DEAD_EPS_MILLI: u32 = 150;

pub const OPTIMISTIC_MILLI: u32 = 900;

pub const ETX_DEAD: u32 = 0;

pub const PENALTY_MAX_MILLI: u32 = 32000;

pub fn etx_milli(df_milli: u32, dr_milli: u32, penalty_milli: u32) -> u32 {
    if (df_milli < DEAD_EPS_MILLI) {
        return ETX_DEAD;
    }
    if (dr_milli < DEAD_EPS_MILLI) {
        return ETX_DEAD;
    }
    let base: u32 = (1000000000 / (df_milli * dr_milli));
    if (penalty_milli > PENALTY_MAX_MILLI) {
        return ((base * PENALTY_MAX_MILLI) / MILLI);
    }
    return ((base * penalty_milli) / MILLI);
}

pub fn better_route(candidate_etx: u32, incumbent_etx: u32) -> bool {
    if (candidate_etx == ETX_DEAD) {
        return false;
    }
    if (incumbent_etx == ETX_DEAD) {
        return true;
    }
    return (candidate_etx < incumbent_etx);
}

pub fn path_etx(upstream_etx: u32, link_etx: u32) -> u32 {
    if (upstream_etx == ETX_DEAD) {
        return ETX_DEAD;
    }
    if (link_etx == ETX_DEAD) {
        return ETX_DEAD;
    }
    return (upstream_etx + link_etx);
}

pub fn is_feasible(new_etx: u32, existing_etx: u32, has_existing: u32) -> bool {
    if (has_existing == 0) {
        return true;
    }
    return better_route(new_etx, existing_etx);
}

pub fn learn_ok(is_self_route: u32, feasible: bool) -> bool {
    if (is_self_route == 1) {
        return false;
    }
    return feasible;
}
