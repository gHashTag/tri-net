// Generated from .t27 spec
// DO NOT EDIT — generated by t27c
// NOT LOWERED BY THIS BACKEND: 8 test(s), 0 invariant(s).
// This backend emits declarations only. The spec's checks live in
// the Zig and Verilog outputs; do not read this file as verified.

pub fn payable(fresh: u32, finite: u32, already_settled: u32) -> u32 {
    if (already_settled == 1) {
        return 0;
    } else {
        if (fresh == 1) {
            if (finite == 1) {
                return 1;
            } else {
                return 0;
            }
        } else {
            return 0;
        }
    }
}

pub fn reward_gate(base_reward: u32, fresh: u32, finite: u32, already_settled: u32) -> u32 {
    if (payable(fresh, finite, already_settled) == 1) {
        return base_reward;
    } else {
        return 0;
    }
}

pub fn payable_authentic(sig_ok: u32, fresh: u32, finite: u32, already_settled: u32) -> u32 {
    if (sig_ok == 1) {
        return payable(fresh, finite, already_settled);
    } else {
        return 0;
    }
}

pub fn reward_gate_authentic(
    base_reward: u32,
    sig_ok: u32,
    fresh: u32,
    finite: u32,
    already_settled: u32,
) -> u32 {
    if (payable_authentic(sig_ok, fresh, finite, already_settled) == 1) {
        return base_reward;
    } else {
        return 0;
    }
}
