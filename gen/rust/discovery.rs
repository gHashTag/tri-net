// Generated from .t27 spec
// DO NOT EDIT — generated by t27c
// NOT LOWERED BY THIS BACKEND: 6 test(s), 2 invariant(s).
// This backend emits declarations only. The spec's checks live in
// the Zig and Verilog outputs; do not read this file as verified.

pub const HDR_LEN: usize = 17;

pub const HEARD_ENTRY_LEN: usize = 4;

pub const MAC_LEN: usize = 16;

pub const FRESHNESS_MS: u64 = 600000;

pub fn mac_offset(n: usize) -> usize {
    return (HDR_LEN + (n * HEARD_ENTRY_LEN));
}

pub fn hello_len(n: usize) -> usize {
    return (mac_offset(n) + MAC_LEN);
}

pub fn parse_len_ok(byte_len: usize, n: usize) -> bool {
    return (byte_len >= hello_len(n));
}

pub fn is_fresh(now_ms: u64, ts_ms: u64) -> bool {
    if (now_ms >= ts_ms) {
        return ((now_ms - ts_ms) <= FRESHNESS_MS);
    } else {
        return ((ts_ms - now_ms) <= FRESHNESS_MS);
    }
}
