// Generated from .t27 spec
// DO NOT EDIT — generated by t27c

pub const GATEWAY_TTL_TICKS: u8 = 10;

pub const MAX_GATEWAYS: u8 = 8;

pub fn mark_gateway(bitmap: u8, node_id: u8) -> u8 {
    if ((node_id < 1) || (node_id > MAX_GATEWAYS)) {
        return bitmap;
    }
    return (bitmap | (1 << (node_id - 1)));
}

pub fn unmark_gateway(bitmap: u8, node_id: u8) -> u8 {
    if ((node_id < 1) || (node_id > MAX_GATEWAYS)) {
        return bitmap;
    }
    return (bitmap & ~((1 << (node_id - 1))));
}

pub fn is_gateway(bitmap: u8, node_id: u8) -> bool {
    if ((node_id < 1) || (node_id > MAX_GATEWAYS)) {
        return false;
    }
    return ((bitmap & (1 << (node_id - 1))) != 0);
}

pub fn gateway_count(bitmap: u8) -> u8 {
    let;
    count;
    let;
    b;
    while (b != 0) {
        if ((b & 1) != 0) {
            count = (count + 1);
        }
        b = (b >> 1);
    }
    return count;
}

pub fn best_gateway(bitmap: u8) -> u8 {
    let;
    i;
    while (i <= MAX_GATEWAYS) {
        if is_gateway(bitmap, i) {
            return i;
        }
        i = (i + 1);
    }
    return 0;
}

pub fn is_expired(age: u8) -> bool {
    return (age > GATEWAY_TTL_TICKS);
}

pub fn tick_age(age: u8) -> u8 {
    if (age >= 255) {
        return 255;
    }
    return (age + 1);
}

pub fn reset_age() -> u8 {
    return 0;
}

pub const GATEWAY_TYPE: u8 = 5;

pub fn is_valid_announcement(type_byte: u8) -> bool {
    return (type_byte == GATEWAY_TYPE);
}

