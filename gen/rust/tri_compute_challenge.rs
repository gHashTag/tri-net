// Generated from .t27 spec
// DO NOT EDIT — generated by t27c
// NOT LOWERED BY THIS BACKEND: 38 test(s), 0 invariant(s).
// This backend emits declarations only. The spec's checks live in
// the Zig and Verilog outputs; do not read this file as verified.

pub const RESOLVE_HONEST: u32 = 0;

pub const RESOLVE_SLASH: u32 = 1;

pub fn is_honest(claimed_result: u32, recomputed_result: u32) -> bool {
    return (claimed_result == recomputed_result);
}

pub fn resolve(claimed_result: u32, recomputed_result: u32) -> u32 {
    if (claimed_result == recomputed_result) {
        return RESOLVE_HONEST;
    } else {
        return RESOLVE_SLASH;
    }
}

pub fn executor_bond_after(prev_bond: u32, outcome: u32) -> u32 {
    if (outcome == RESOLVE_SLASH) {
        return 0;
    } else {
        return prev_bond;
    }
}

pub fn challenger_reward(bond: u32, outcome: u32) -> u32 {
    if (outcome == RESOLVE_SLASH) {
        return bond;
    } else {
        return 0;
    }
}

pub fn challenger_stake_after(stake: u32, outcome: u32) -> u32 {
    if (outcome == RESOLVE_HONEST) {
        return 0;
    } else {
        return stake;
    }
}

pub const RESOLVE_MALFORMED: u32 = 2;

pub fn challenge_binds(settled_leaf: u32, dispute_leaf: u32) -> bool {
    return (settled_leaf == dispute_leaf);
}

pub fn resolve_bound(
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    recomputed_result: u32,
) -> u32 {
    if (settled_leaf != dispute_leaf) {
        return RESOLVE_MALFORMED;
    } else {
        if (claimed_result == recomputed_result) {
            return RESOLVE_HONEST;
        } else {
            return RESOLVE_SLASH;
        }
    }
}

pub fn challenger_stake_after_bound(stake: u32, outcome: u32) -> u32 {
    if (outcome == RESOLVE_SLASH) {
        return stake;
    } else {
        if (outcome == RESOLVE_STALE) {
            return stake;
        } else {
            if (outcome == RESOLVE_INDETERMINATE) {
                return stake;
            } else {
                return 0;
            }
        }
    }
}

pub const RESOLVE_STALE: u32 = 3;

pub fn dispute_is_fresh(last_epoch: u32, dispute_epoch: u32) -> bool {
    return (dispute_epoch > last_epoch);
}

pub fn resolver_epoch_after(last_epoch: u32, dispute_epoch: u32, outcome: u32) -> u32 {
    if (outcome == RESOLVE_HONEST) {
        return dispute_epoch;
    } else {
        if (outcome == RESOLVE_SLASH) {
            return dispute_epoch;
        } else {
            return last_epoch;
        }
    }
}

pub const FMT_GF_BINARY: u32 = 0;

pub const FMT_GFT: u32 = 1;

pub const RESOLVE_FAMILY_MISMATCH: u32 = 4;

pub fn family_matches(settled_family: u32, dispute_family: u32) -> bool {
    return (settled_family == dispute_family);
}

pub fn resolve_full(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    recomputed_result: u32,
) -> u32 {
    if (dispute_epoch > last_epoch) {
        if (settled_family == dispute_family) {
            return resolve_bound(
                settled_leaf,
                dispute_leaf,
                claimed_result,
                recomputed_result,
            );
        } else {
            return RESOLVE_FAMILY_MISMATCH;
        }
    } else {
        return RESOLVE_STALE;
    }
}

pub const RESOLVE_RUNG_MISMATCH: u32 = 6;

pub fn rung_matches(settled_et: u32, dispute_et: u32) -> bool {
    return (settled_et == dispute_et);
}

pub fn resolve_full_rung(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    settled_et: u32,
    dispute_et: u32,
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    recomputed_result: u32,
) -> u32 {
    if (dispute_epoch > last_epoch) {
        if (settled_family == dispute_family) {
            if (settled_et == dispute_et) {
                return resolve_bound(
                    settled_leaf,
                    dispute_leaf,
                    claimed_result,
                    recomputed_result,
                );
            } else {
                return RESOLVE_RUNG_MISMATCH;
            }
        } else {
            return RESOLVE_FAMILY_MISMATCH;
        }
    } else {
        return RESOLVE_STALE;
    }
}

pub fn resolve_bound_d256(leaf_match: u32, claimed_result: u32, recomputed_result: u32) -> u32 {
    if (leaf_match == 1) {
        if (claimed_result == recomputed_result) {
            return RESOLVE_HONEST;
        } else {
            return RESOLVE_SLASH;
        }
    } else {
        return RESOLVE_MALFORMED;
    }
}

pub fn resolve_full_d256(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    leaf_match: u32,
    claimed_result: u32,
    recomputed_result: u32,
) -> u32 {
    if (dispute_epoch > last_epoch) {
        if (settled_family == dispute_family) {
            return resolve_bound_d256(leaf_match, claimed_result, recomputed_result);
        } else {
            return RESOLVE_FAMILY_MISMATCH;
        }
    } else {
        return RESOLVE_STALE;
    }
}

pub fn resolve_bitnet(
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    recomputed_result: u32,
    ternary_ok: u32,
) -> u32 {
    if (settled_leaf == dispute_leaf) {
        if (ternary_ok == 1) {
            return resolve_bound(
                settled_leaf,
                dispute_leaf,
                claimed_result,
                recomputed_result,
            );
        } else {
            return RESOLVE_SLASH;
        }
    } else {
        return RESOLVE_MALFORMED;
    }
}

pub fn resolve_bitnet_full(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    recomputed_result: u32,
    ternary_ok: u32,
) -> u32 {
    if (dispute_epoch > last_epoch) {
        if (settled_family == dispute_family) {
            return resolve_bitnet(
                settled_leaf,
                dispute_leaf,
                claimed_result,
                recomputed_result,
                ternary_ok,
            );
        } else {
            return RESOLVE_FAMILY_MISMATCH;
        }
    } else {
        return RESOLVE_STALE;
    }
}

pub fn resolve_bitnet_d256(
    leaf_match: u32,
    claimed_result: u32,
    recomputed_result: u32,
    ternary_ok: u32,
) -> u32 {
    if (leaf_match == 1) {
        if (ternary_ok == 1) {
            if (claimed_result == recomputed_result) {
                return RESOLVE_HONEST;
            } else {
                return RESOLVE_SLASH;
            }
        } else {
            return RESOLVE_SLASH;
        }
    } else {
        return RESOLVE_MALFORMED;
    }
}

pub fn resolve_bitnet_d256_full(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    leaf_match: u32,
    claimed_result: u32,
    recomputed_result: u32,
    ternary_ok: u32,
) -> u32 {
    if (dispute_epoch > last_epoch) {
        if (settled_family == dispute_family) {
            return resolve_bitnet_d256(leaf_match, claimed_result, recomputed_result, ternary_ok);
        } else {
            return RESOLVE_FAMILY_MISMATCH;
        }
    } else {
        return RESOLVE_STALE;
    }
}

pub fn majority_flag3(a: u32, b: u32, c: u32) -> u32 {
    if (((a + b) + c) >= 2) {
        return 1;
    } else {
        return 0;
    }
}

pub fn resolve_bitnet_quorum(
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    v0: u32,
    v1: u32,
    v2: u32,
    t0: u32,
    t1: u32,
    t2: u32,
) -> u32 {
    if (verifier_quorum3(v0, v1, v2) == 1) {
        return resolve_bitnet(
            settled_leaf,
            dispute_leaf,
            claimed_result,
            quorum_result3(v0, v1, v2),
            majority_flag3(t0, t1, t2),
        );
    } else {
        return RESOLVE_INDETERMINATE;
    }
}

pub fn majority_flag5(a: u32, b: u32, c: u32, d: u32, e: u32) -> u32 {
    if (((((a + b) + c) + d) + e) >= 3) {
        return 1;
    } else {
        return 0;
    }
}

pub fn resolve_bitnet_quorum5(
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    v0: u32,
    v1: u32,
    v2: u32,
    v3: u32,
    v4: u32,
    t0: u32,
    t1: u32,
    t2: u32,
    t3: u32,
    t4: u32,
) -> u32 {
    if (has_quorum_k(max_agree5(v0, v1, v2, v3, v4), quorum_threshold(5)) == 1) {
        return resolve_bitnet(
            settled_leaf,
            dispute_leaf,
            claimed_result,
            quorum_value5(v0, v1, v2, v3, v4, quorum_threshold(5)),
            majority_flag5(t0, t1, t2, t3, t4),
        );
    } else {
        return RESOLVE_INDETERMINATE;
    }
}

pub fn resolve_bitnet_quorum_full(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    v0: u32,
    v1: u32,
    v2: u32,
    t0: u32,
    t1: u32,
    t2: u32,
) -> u32 {
    if (verifier_quorum3(v0, v1, v2) == 1) {
        return resolve_bitnet_full(
            last_epoch,
            dispute_epoch,
            settled_family,
            dispute_family,
            settled_leaf,
            dispute_leaf,
            claimed_result,
            quorum_result3(v0, v1, v2),
            majority_flag3(t0, t1, t2),
        );
    } else {
        return RESOLVE_INDETERMINATE;
    }
}

pub fn resolve_bitnet_quorum5_full(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    v0: u32,
    v1: u32,
    v2: u32,
    v3: u32,
    v4: u32,
    t0: u32,
    t1: u32,
    t2: u32,
    t3: u32,
    t4: u32,
) -> u32 {
    if (has_quorum_k(max_agree5(v0, v1, v2, v3, v4), quorum_threshold(5)) == 1) {
        return resolve_bitnet_full(
            last_epoch,
            dispute_epoch,
            settled_family,
            dispute_family,
            settled_leaf,
            dispute_leaf,
            claimed_result,
            quorum_value5(v0, v1, v2, v3, v4, quorum_threshold(5)),
            majority_flag5(t0, t1, t2, t3, t4),
        );
    } else {
        return RESOLVE_INDETERMINATE;
    }
}

pub const RESOLVE_INDETERMINATE: u32 = 5;

pub fn verifier_quorum3(v0: u32, v1: u32, v2: u32) -> u32 {
    if (v0 == v1) {
        return 1;
    }
    if (v0 == v2) {
        return 1;
    }
    if (v1 == v2) {
        return 1;
    }
    return 0;
}

pub fn quorum_result3(v0: u32, v1: u32, v2: u32) -> u32 {
    if (v0 == v1) {
        return v0;
    }
    if (v0 == v2) {
        return v0;
    }
    if (v1 == v2) {
        return v1;
    }
    return v0;
}

pub fn resolve_quorum3(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    v0: u32,
    v1: u32,
    v2: u32,
) -> u32 {
    if (verifier_quorum3(v0, v1, v2) == 1) {
        return resolve_full(
            last_epoch,
            dispute_epoch,
            settled_family,
            dispute_family,
            settled_leaf,
            dispute_leaf,
            claimed_result,
            quorum_result3(v0, v1, v2),
        );
    } else {
        return RESOLVE_INDETERMINATE;
    }
}

pub fn verifier_dissented(vote: u32, quorum_value: u32, has_quorum: u32) -> u32 {
    if (has_quorum == 1) {
        if (vote == quorum_value) {
            return 0;
        } else {
            return 1;
        }
    } else {
        return 0;
    }
}

pub fn verifier_stake_after(stake: u32, dissented: u32) -> u32 {
    if (dissented == 1) {
        return 0;
    } else {
        return stake;
    }
}

pub fn verifier_reward(burned_total: u32, honest_count: u32) -> u32 {
    if (honest_count == 0) {
        return 0;
    } else {
        let share: u64 = ((burned_total as u64) / (honest_count as u64));
        return (share as u32);
    }
}

pub fn quorum_threshold(n: u32) -> u32 {
    return ((n >> 1) + 1);
}

pub fn has_quorum_k(agree_count: u32, k: u32) -> u32 {
    if (agree_count >= k) {
        return 1;
    } else {
        return 0;
    }
}

pub fn max_agree3(v0: u32, v1: u32, v2: u32) -> u32 {
    if (v0 == v1) {
        if (v1 == v2) {
            return 3;
        } else {
            return 2;
        }
    } else {
        if (v0 == v2) {
            return 2;
        } else {
            if (v1 == v2) {
                return 2;
            } else {
                return 1;
            }
        }
    }
}

pub fn eq1(a: u32, b: u32) -> u32 {
    if (a == b) {
        return 1;
    } else {
        return 0;
    }
}

pub fn max2(a: u32, b: u32) -> u32 {
    if (a >= b) {
        return a;
    } else {
        return b;
    }
}

pub fn count5(x: u32, v0: u32, v1: u32, v2: u32, v3: u32, v4: u32) -> u32 {
    return ((((eq1(x, v0) + eq1(x, v1)) + eq1(x, v2)) + eq1(x, v3)) + eq1(x, v4));
}

pub fn max_agree5(v0: u32, v1: u32, v2: u32, v3: u32, v4: u32) -> u32 {
    let c0: u32 = count5(v0, v0, v1, v2, v3, v4);
    let c1: u32 = count5(v1, v0, v1, v2, v3, v4);
    let c2: u32 = count5(v2, v0, v1, v2, v3, v4);
    let c3: u32 = count5(v3, v0, v1, v2, v3, v4);
    let c4: u32 = count5(v4, v0, v1, v2, v3, v4);
    return max2(max2(max2(c0, c1), max2(c2, c3)), c4);
}

pub fn quorum_value5(v0: u32, v1: u32, v2: u32, v3: u32, v4: u32, k: u32) -> u32 {
    if (count5(v0, v0, v1, v2, v3, v4) >= k) {
        return v0;
    }
    if (count5(v1, v0, v1, v2, v3, v4) >= k) {
        return v1;
    }
    if (count5(v2, v0, v1, v2, v3, v4) >= k) {
        return v2;
    }
    if (count5(v3, v0, v1, v2, v3, v4) >= k) {
        return v3;
    }
    if (count5(v4, v0, v1, v2, v3, v4) >= k) {
        return v4;
    }
    return v0;
}

pub fn resolve_quorum5(
    last_epoch: u32,
    dispute_epoch: u32,
    settled_family: u32,
    dispute_family: u32,
    settled_leaf: u32,
    dispute_leaf: u32,
    claimed_result: u32,
    v0: u32,
    v1: u32,
    v2: u32,
    v3: u32,
    v4: u32,
) -> u32 {
    if (has_quorum_k(max_agree5(v0, v1, v2, v3, v4), quorum_threshold(5)) == 1) {
        return resolve_full(
            last_epoch,
            dispute_epoch,
            settled_family,
            dispute_family,
            settled_leaf,
            dispute_leaf,
            claimed_result,
            quorum_value5(v0, v1, v2, v3, v4, quorum_threshold(5)),
        );
    } else {
        return RESOLVE_INDETERMINATE;
    }
}

pub fn dissenter_count5(v0: u32, v1: u32, v2: u32, v3: u32, v4: u32) -> u32 {
    return (5 - max_agree5(v0, v1, v2, v3, v4));
}

pub fn burned_total5(v0: u32, v1: u32, v2: u32, v3: u32, v4: u32, stake: u32) -> u32 {
    let prod: u64 = ((dissenter_count5(v0, v1, v2, v3, v4) as u64) * (stake as u64));
    if (prod > 4294967295) {
        return 4294967295;
    } else {
        return (prod as u32);
    }
}

pub fn honest_share5(v0: u32, v1: u32, v2: u32, v3: u32, v4: u32, stake: u32) -> u32 {
    return verifier_reward(
        burned_total5(v0, v1, v2, v3, v4, stake),
        max_agree5(v0, v1, v2, v3, v4),
    );
}
