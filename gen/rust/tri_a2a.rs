// Generated from .t27 spec
// DO NOT EDIT — generated by t27c
// NOT LOWERED BY THIS BACKEND: 22 test(s), 0 invariant(s).
// This backend emits declarations only. The spec's checks live in
// the Zig and Verilog outputs; do not read this file as verified.

pub const KIND_DATA: u32 = 1;

pub const A2A_PORT: u32 = 0xA2A;

pub const MSG_TASK_ASSIGN: u32 = 1;

pub const MSG_TASK_RESULT: u32 = 2;

pub const MSG_HEARTBEAT: u32 = 3;

pub const SKILL_GF16_MUL: u32 = 0x1611;

pub const SKILL_GF16_ADD: u32 = 0x1610;

pub const SKILL_GFT16_MUL: u32 = 0xA611;

pub const SKILL_GFT16_ADD: u32 = 0xA610;

pub const SKILL_GFT8_MUL: u32 = 0xA811;

pub const SKILL_GFT8_ADD: u32 = 0xA810;

pub const SKILL_GFT4_MUL: u32 = 0xA411;

pub const SKILL_GFT4_ADD: u32 = 0xA410;

pub const SKILL_GFT32_MUL: u32 = 0xA511;

pub const SKILL_GFT32_ADD: u32 = 0xA510;

pub const SKILL_GFT64_MUL: u32 = 0xA311;

pub const SKILL_GFT64_ADD: u32 = 0xA310;

pub const SKILL_GFT128_MUL: u32 = 0xA211;

pub const SKILL_GFT128_ADD: u32 = 0xA210;

pub fn is_a2a(kind: u32, port: u32) -> bool {
    if (kind == KIND_DATA) {
        return (port == A2A_PORT);
    } else {
        return (port != port);
    }
}

pub fn carries_receipt(msg_class: u32) -> bool {
    return (msg_class == MSG_TASK_RESULT);
}

pub fn is_gf_skill(skill: u32) -> bool {
    if (skill == SKILL_GF16_MUL) {
        return (skill == skill);
    } else {
        return (skill == SKILL_GF16_ADD);
    }
}

pub fn is_gft_skill(skill: u32) -> bool {
    if (skill == SKILL_GFT16_MUL) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT16_ADD) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT8_MUL) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT8_ADD) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT4_MUL) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT4_ADD) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT32_MUL) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT32_ADD) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT64_MUL) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT64_ADD) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT128_MUL) {
        return (skill == skill);
    }
    if (skill == SKILL_GFT128_ADD) {
        return (skill == skill);
    }
    return (skill != skill);
}

pub const FMT_GF_BINARY: u32 = 0;

pub const FMT_GFT: u32 = 1;

pub fn skill_family(skill: u32) -> u32 {
    if (skill == SKILL_GFT16_MUL) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT16_ADD) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT8_MUL) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT8_ADD) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT4_MUL) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT4_ADD) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT32_MUL) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT32_ADD) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT64_MUL) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT64_ADD) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT128_MUL) {
        return FMT_GFT;
    }
    if (skill == SKILL_GFT128_ADD) {
        return FMT_GFT;
    }
    return FMT_GF_BINARY;
}

pub fn family_matches(assigned_skill: u32, receipt_family: u32) -> bool {
    return (skill_family(assigned_skill) == receipt_family);
}

pub fn is_hosted_skill(skill: u32) -> u32 {
    if (skill == SKILL_GF16_MUL) {
        return 1;
    }
    if (skill == SKILL_GF16_ADD) {
        return 1;
    }
    if (skill == SKILL_GFT16_MUL) {
        return 1;
    }
    if (skill == SKILL_GFT16_ADD) {
        return 1;
    }
    if (skill == SKILL_GFT8_MUL) {
        return 1;
    }
    if (skill == SKILL_GFT8_ADD) {
        return 1;
    }
    if (skill == SKILL_GFT4_MUL) {
        return 1;
    }
    if (skill == SKILL_GFT4_ADD) {
        return 1;
    }
    if (skill == SKILL_GFT32_MUL) {
        return 1;
    }
    if (skill == SKILL_GFT32_ADD) {
        return 1;
    }
    if (skill == SKILL_GFT64_MUL) {
        return 1;
    }
    if (skill == SKILL_GFT64_ADD) {
        return 1;
    }
    if (skill == SKILL_GFT128_MUL) {
        return 1;
    }
    if (skill == SKILL_GFT128_ADD) {
        return 1;
    }
    return 0;
}

pub fn family_matches_strict(assigned_skill: u32, receipt_family: u32) -> bool {
    if (is_hosted_skill(assigned_skill) == 1) {
        return (skill_family(assigned_skill) == receipt_family);
    } else {
        return (assigned_skill != assigned_skill);
    }
}

pub fn skill_op(skill: u32) -> u32 {
    return (skill & 0xFF);
}

pub fn result_matches_assign(assign_task_id: u32, result_task_id: u32, receipt_task: u32) -> bool {
    if (assign_task_id == result_task_id) {
        return (result_task_id == receipt_task);
    } else {
        return (result_task_id != result_task_id);
    }
}

pub fn is_fresh(task_id: u32, last_settled: u32) -> bool {
    return (task_id > last_settled);
}

pub fn next_watermark(task_id: u32, last_settled: u32) -> u32 {
    if (task_id > last_settled) {
        return task_id;
    } else {
        return last_settled;
    }
}

pub fn next_watermark_settled(task_id: u32, last_settled: u32, settled: u32) -> u32 {
    if (settled == 1) {
        if (task_id > last_settled) {
            return task_id;
        } else {
            return last_settled;
        }
    } else {
        return last_settled;
    }
}

pub fn op_matches(assigned_skill: u32, receipt_op: u32) -> bool {
    return (skill_op(assigned_skill) == receipt_op);
}

pub fn result_binds_assign(
    assign_task_id: u32,
    result_task_id: u32,
    receipt_task: u32,
    assigned_skill: u32,
    receipt_family: u32,
    receipt_op: u32,
) -> bool {
    if (is_hosted_skill(assigned_skill) == 1) {
        if (assign_task_id == result_task_id) {
            if (result_task_id == receipt_task) {
                if (skill_family(assigned_skill) == receipt_family) {
                    return (skill_op(assigned_skill) == receipt_op);
                } else {
                    return (receipt_family != receipt_family);
                }
            } else {
                return (receipt_task != receipt_task);
            }
        } else {
            return (assign_task_id != assign_task_id);
        }
    } else {
        return (assigned_skill != assigned_skill);
    }
}

pub fn skill_width(skill: u32) -> u32 {
    if (skill == SKILL_GFT8_MUL) {
        return 8;
    }
    if (skill == SKILL_GFT8_ADD) {
        return 8;
    }
    if (skill == SKILL_GFT4_MUL) {
        return 4;
    }
    if (skill == SKILL_GFT4_ADD) {
        return 4;
    }
    if (skill == SKILL_GFT32_MUL) {
        return 32;
    }
    if (skill == SKILL_GFT32_ADD) {
        return 32;
    }
    if (skill == SKILL_GFT64_MUL) {
        return 64;
    }
    if (skill == SKILL_GFT64_ADD) {
        return 64;
    }
    if (skill == SKILL_GFT128_MUL) {
        return 128;
    }
    if (skill == SKILL_GFT128_ADD) {
        return 128;
    }
    return 16;
}

pub fn skill_et(skill: u32) -> u32 {
    if (skill == SKILL_GFT16_MUL) {
        return 4;
    }
    if (skill == SKILL_GFT16_ADD) {
        return 4;
    }
    if (skill == SKILL_GFT8_MUL) {
        return 3;
    }
    if (skill == SKILL_GFT8_ADD) {
        return 3;
    }
    if (skill == SKILL_GFT4_MUL) {
        return 2;
    }
    if (skill == SKILL_GFT4_ADD) {
        return 2;
    }
    if (skill == SKILL_GFT32_MUL) {
        return 6;
    }
    if (skill == SKILL_GFT32_ADD) {
        return 6;
    }
    if (skill == SKILL_GFT64_MUL) {
        return 9;
    }
    if (skill == SKILL_GFT64_ADD) {
        return 9;
    }
    if (skill == SKILL_GFT128_MUL) {
        return 14;
    }
    if (skill == SKILL_GFT128_ADD) {
        return 14;
    }
    return 0;
}

pub fn result_binds_assign_sized(
    assign_task_id: u32,
    result_task_id: u32,
    receipt_task: u32,
    assigned_skill: u32,
    receipt_family: u32,
    receipt_op: u32,
    receipt_width: u32,
) -> bool {
    if (skill_width(assigned_skill) == receipt_width) {
        return result_binds_assign(
            assign_task_id,
            result_task_id,
            receipt_task,
            assigned_skill,
            receipt_family,
            receipt_op,
        );
    } else {
        return (receipt_width != receipt_width);
    }
}

pub fn result_binds_assign_rung(
    assign_task_id: u32,
    result_task_id: u32,
    receipt_task: u32,
    assigned_skill: u32,
    receipt_family: u32,
    receipt_op: u32,
    receipt_width: u32,
    receipt_et: u32,
) -> bool {
    if (skill_et(assigned_skill) == receipt_et) {
        return result_binds_assign_sized(
            assign_task_id,
            result_task_id,
            receipt_task,
            assigned_skill,
            receipt_family,
            receipt_op,
            receipt_width,
        );
    } else {
        return (receipt_et != receipt_et);
    }
}

pub fn admit_result(
    assign_task_id: u32,
    result_task_id: u32,
    receipt_task: u32,
    assigned_skill: u32,
    receipt_family: u32,
    receipt_op: u32,
    last_settled: u32,
    exec_rep: u32,
    min_rep: u32,
) -> bool {
    if (result_task_id > last_settled) {
        if (exec_rep >= min_rep) {
            return result_binds_assign(
                assign_task_id,
                result_task_id,
                receipt_task,
                assigned_skill,
                receipt_family,
                receipt_op,
            );
        } else {
            return (exec_rep != exec_rep);
        }
    } else {
        return (result_task_id != result_task_id);
    }
}

pub const A2A_BOND_BPS_UNIT: u32 = 10000;

pub fn admit_result_bonded(
    assign_task_id: u32,
    result_task_id: u32,
    receipt_task: u32,
    assigned_skill: u32,
    receipt_family: u32,
    receipt_op: u32,
    last_settled: u32,
    exec_rep: u32,
    min_rep: u32,
    bond: u32,
    outstanding: u32,
    min_bps: u32,
) -> bool {
    let need: u64 = ((outstanding as u64) * (min_bps as u64));
    let required: u32 = ((need / (A2A_BOND_BPS_UNIT as u64)) as u32);
    if (bond >= required) {
        return admit_result(
            assign_task_id,
            result_task_id,
            receipt_task,
            assigned_skill,
            receipt_family,
            receipt_op,
            last_settled,
            exec_rep,
            min_rep,
        );
    } else {
        return (bond != bond);
    }
}

pub fn executor_binds_assign(assigned_executor: u32, receipt_executor: u32) -> bool {
    return (assigned_executor == receipt_executor);
}

pub fn admit_result_authentic(
    assign_task_id: u32,
    result_task_id: u32,
    receipt_task: u32,
    assigned_skill: u32,
    receipt_family: u32,
    receipt_op: u32,
    last_settled: u32,
    exec_rep: u32,
    min_rep: u32,
    bond: u32,
    outstanding: u32,
    min_bps: u32,
    assigned_executor: u32,
    receipt_executor: u32,
) -> bool {
    if (assigned_executor == receipt_executor) {
        return admit_result_bonded(
            assign_task_id,
            result_task_id,
            receipt_task,
            assigned_skill,
            receipt_family,
            receipt_op,
            last_settled,
            exec_rep,
            min_rep,
            bond,
            outstanding,
            min_bps,
        );
    } else {
        return (assigned_executor != assigned_executor);
    }
}

pub fn executor_authentic(
    assigned_executor: u32,
    receipt_executor: u32,
    sig_ok: u32,
    hashed_pubkey_lo: u32,
) -> bool {
    if (assigned_executor == receipt_executor) {
        if (sig_ok == 1) {
            return (receipt_executor == hashed_pubkey_lo);
        } else {
            return (sig_ok != sig_ok);
        }
    } else {
        return (assigned_executor != assigned_executor);
    }
}

pub fn admit_result_signed(
    assign_task_id: u32,
    result_task_id: u32,
    receipt_task: u32,
    assigned_skill: u32,
    receipt_family: u32,
    receipt_op: u32,
    last_settled: u32,
    exec_rep: u32,
    min_rep: u32,
    bond: u32,
    outstanding: u32,
    min_bps: u32,
    assigned_executor: u32,
    receipt_executor: u32,
    sig_ok: u32,
    hashed_pubkey_lo: u32,
) -> bool {
    if (assigned_executor == receipt_executor) {
        if (sig_ok == 1) {
            if (receipt_executor == hashed_pubkey_lo) {
                return admit_result_bonded(
                    assign_task_id,
                    result_task_id,
                    receipt_task,
                    assigned_skill,
                    receipt_family,
                    receipt_op,
                    last_settled,
                    exec_rep,
                    min_rep,
                    bond,
                    outstanding,
                    min_bps,
                );
            } else {
                return (receipt_executor != receipt_executor);
            }
        } else {
            return (sig_ok != sig_ok);
        }
    } else {
        return (assigned_executor != assigned_executor);
    }
}

pub fn admit_result_signed_sized(
    assign_task_id: u32,
    result_task_id: u32,
    receipt_task: u32,
    assigned_skill: u32,
    receipt_family: u32,
    receipt_op: u32,
    last_settled: u32,
    exec_rep: u32,
    min_rep: u32,
    bond: u32,
    outstanding: u32,
    min_bps: u32,
    assigned_executor: u32,
    receipt_executor: u32,
    sig_ok: u32,
    hashed_pubkey_lo: u32,
    receipt_width: u32,
) -> bool {
    if (skill_width(assigned_skill) == receipt_width) {
        return admit_result_signed(
            assign_task_id,
            result_task_id,
            receipt_task,
            assigned_skill,
            receipt_family,
            receipt_op,
            last_settled,
            exec_rep,
            min_rep,
            bond,
            outstanding,
            min_bps,
            assigned_executor,
            receipt_executor,
            sig_ok,
            hashed_pubkey_lo,
        );
    } else {
        return (receipt_width != receipt_width);
    }
}
