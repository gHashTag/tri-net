// Generated from .t27 spec
// DO NOT EDIT — generated by t27c
// NOT LOWERED BY THIS BACKEND: 13 test(s), 0 invariant(s).
// This backend emits declarations only. The spec's checks live in
// the Zig and Verilog outputs; do not read this file as verified.

pub fn total_work3(w0: u32, w1: u32, w2: u32) -> u32 {
    let sum: u64 = (((w0 as u64) + (w1 as u64)) + (w2 as u64));
    if (sum > 4294967295) {
        return 4294967295;
    } else {
        return (sum as u32);
    }
}

pub fn pool_share(total_pool: u32, my_work: u32, total: u32) -> u32 {
    if (total == 0) {
        return 0;
    } else {
        let num: u64 = ((total_pool as u64) * (my_work as u64));
        let den: u64 = (total as u64);
        return ((num / den) as u32);
    }
}

pub fn pool_after_deposit(pool: u32, amount: u32) -> u32 {
    let sum: u32 = (pool).wrapping_add(amount);
    if (sum < pool) {
        return 0xFFFFFFFF;
    } else {
        return sum;
    }
}

pub fn payout_capped(pool: u32, requested: u32) -> u32 {
    if (requested <= pool) {
        return requested;
    } else {
        return pool;
    }
}

pub fn pool_after_payout(pool: u32, requested: u32) -> u32 {
    if (requested <= pool) {
        return (pool - requested);
    } else {
        return 0;
    }
}

pub fn balance_after_pool_settle(prev_balance: u32, pool: u32, reward: u32) -> u32 {
    let credit: u32 = payout_capped(pool, reward);
    let sum: u32 = (prev_balance).wrapping_add(credit);
    if (sum < prev_balance) {
        return 0xFFFFFFFF;
    } else {
        return sum;
    }
}
