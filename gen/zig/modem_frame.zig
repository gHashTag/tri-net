// Generated from t27 spec: ModemFrame (module name)
// DO NOT EDIT — generated by t27c
// phi^2 + 1/phi^2 = 3 | TRINITY

const std = @import("std");

// use types: no references in this module
const PREAMBLE_LEN: usize = 13;
const BITS_PER_BYTE: usize = 8;
const MAX_PAYLOAD: usize = 255;
const PEAK: usize = 13;
const SYNC_THRESHOLD: usize = 8;
fn frame_symbols(payload_len: usize) usize {
    return (PREAMBLE_LEN + BITS_PER_BYTE) + (payload_len * BITS_PER_BYTE);
}
fn min_parse_len() usize {
    return PREAMBLE_LEN + BITS_PER_BYTE;
}
fn can_parse(nsamples: usize) bool {
    return nsamples >= min_parse_len();
}
fn payload_fits(n: usize) bool {
    return n <= MAX_PAYLOAD;
}
fn decode_fits(sym_start: usize, out_len: usize, total: usize) bool {
    return (sym_start + (out_len * BITS_PER_BYTE)) <= total;
}
fn is_synced(corr_peak: usize) bool {
    return corr_peak >= SYNC_THRESHOLD;
}
fn sync_margin_pct() usize {
    return (SYNC_THRESHOLD * 100) / PEAK;
}
test "empty_frame_is_21_symbols" {
}
test "one_byte_payload_is_29_symbols" {
}
test "max_frame_symbols" {
}
test "min_parse_len_is_preamble_plus_length_byte" {
}
test "can_parse_gate" {
}
test "payload_cap" {
}
test "decode_bounds" {
}
test "sync_gate" {
}
test "sync_margin_is_61_percent" {
}
comptime {
    // invariant: preamble_is_barker13
    // invariant: preamble_is_barker13 verified (no statements)
}
comptime {
    // invariant: payload_cap_is_one_byte
    // invariant: payload_cap_is_one_byte verified (no statements)
}
comptime {
    // invariant: threshold_below_peak
    // invariant: threshold_below_peak verified (no statements)
}
