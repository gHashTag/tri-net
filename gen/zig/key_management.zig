// Generated from t27 spec: KeyManagement (module name)
// DO NOT EDIT — generated by t27c
// phi^2 + 1/phi^2 = 3 | TRINITY

const std = @import("std");

const types = @import("types.zig");

const MAX_KEYS: u32 = 4;
const KEY_SIZE: u32 = 4;
const KEY_VALID: u32 = 1;
const KEY_INVALID: u32 = 0;
const ROTATION_INTERVAL: u32 = 30000;
fn create_key_entry(valid: u32, key_id: u32, key_value: u32, timestamp: u32) u32 {
    return ((((valid & 0x1) << 31) | ((key_id & 0xFF) << 24)) | ((key_value & 0xFFFF) << 8)) | (timestamp & 0xFF);
}
fn get_key_valid(entry: u32) u32 {
    return (entry >> 31) & 0x1;
}
fn get_key_id(entry: u32) u32 {
    return (entry >> 24) & 0xFF;
}
fn get_key_value(entry: u32) u32 {
    return (entry >> 8) & 0xFFFF;
}
fn get_key_timestamp(entry: u32) u32 {
    return entry & 0xFF;
}
fn create_key_store(k0: u32, k1: u32, k2: u32, k3: u32) u64 {
    return (((@as(u64, @intCast(k0)) << 48) | (@as(u64, @intCast(k1)) << 32)) | (@as(u64, @intCast(k2)) << 16)) | @as(u64, @intCast(k3));
}
fn get_key_entry(store: u64, index: u32) u32 {
    if (index == 0) {
        return @as(u32, @intCast((store >> 48) & 0xFFFFFFFF));
    }
    if (index == 1) {
        return @as(u32, @intCast((store >> 32) & 0xFFFFFFFF));
    }
    if (index == 2) {
        return @as(u32, @intCast((store >> 16) & 0xFFFFFFFF));
    }
    return @as(u32, @intCast(store & 0xFFFFFFFF));
}
fn find_key_by_id(store: u64, key_id: u32) u32 {
    if ((get_key_id(get_key_entry(store, 0)) == key_id) and (get_key_valid(get_key_entry(store, 0)) == KEY_VALID)) {
        return 0;
    } else if ((get_key_id(get_key_entry(store, 1)) == key_id) and (get_key_valid(get_key_entry(store, 1)) == KEY_VALID)) {
        return 1;
    } else if ((get_key_id(get_key_entry(store, 2)) == key_id) and (get_key_valid(get_key_entry(store, 2)) == KEY_VALID)) {
        return 2;
    } else if ((get_key_id(get_key_entry(store, 3)) == key_id) and (get_key_valid(get_key_entry(store, 3)) == KEY_VALID)) {
        return 3;
    }
    return 0xFF;
}
fn add_key(store: u64, key_id: u32, key_value: u32, timestamp: u32) u64 {
    if (get_key_valid(get_key_entry(store, 0)) == KEY_INVALID) {
        return (store & 0x0000FFFFFFFFFFFF) | (@as(u64, @intCast(create_key_entry(KEY_VALID, key_id, key_value, timestamp))) << 48);
    } else if (get_key_valid(get_key_entry(store, 1)) == KEY_INVALID) {
        return (store & 0xFFFF0000FFFFFFFF) | (@as(u64, @intCast(create_key_entry(KEY_VALID, key_id, key_value, timestamp))) << 32);
    } else if (get_key_valid(get_key_entry(store, 2)) == KEY_INVALID) {
        return (store & 0xFFFFFFFF0000FFFF) | (@as(u64, @intCast(create_key_entry(KEY_VALID, key_id, key_value, timestamp))) << 16);
    } else if (get_key_valid(get_key_entry(store, 3)) == KEY_INVALID) {
        return (store & 0xFFFFFFFFFFFF0000) | @as(u64, @intCast(create_key_entry(KEY_VALID, key_id, key_value, timestamp)));
    }
    return store;
}
fn invalidate_key(store: u64, key_id: u32) u64 {
    if (index != 0xFF) {
        if (index == 0) {
            return (store & 0x0000FFFFFFFFFFFF) | (@as(u64, @intCast(new_entry)) << 48);
        } else if (index == 1) {
            return (store & 0xFFFF0000FFFFFFFF) | (@as(u64, @intCast(new_entry)) << 32);
        } else if (index == 2) {
            return (store & 0xFFFFFFFF0000FFFF) | (@as(u64, @intCast(new_entry)) << 16);
        } else {
            return (store & 0xFFFFFFFFFFFF0000) | @as(u64, @intCast(new_entry));
        }
    }
    return store;
}
fn needs_rotation(entry: u32, current_time: u32) bool {
    if (get_key_valid(entry) == KEY_INVALID) {
        return false;
    }
    return age >= ROTATION_INTERVAL;
}
fn rotate_key(store: u64, key_id: u32, new_value: u32, current_time: u32) u64 {
    if (index != 0xFF) {
        if (index == 0) {
            return (store & 0x0000FFFFFFFFFFFF) | (@as(u64, @intCast(new_entry)) << 48);
        } else if (index == 1) {
            return (store & 0xFFFF0000FFFFFFFF) | (@as(u64, @intCast(new_entry)) << 32);
        } else if (index == 2) {
            return (store & 0xFFFFFFFF0000FFFF) | (@as(u64, @intCast(new_entry)) << 16);
        } else {
            return (store & 0xFFFFFFFFFFFF0000) | @as(u64, @intCast(new_entry));
        }
    }
    return store;
}
fn get_active_key(store: u64) u32 {
    if (get_key_valid(get_key_entry(store, 0)) == KEY_VALID) {
        if (ts >= best_timestamp) {
            best_timestamp = ts;
            best_index = 0;
        }
    }
    if (get_key_valid(get_key_entry(store, 1)) == KEY_VALID) {
        if (ts >= best_timestamp) {
            best_timestamp = ts;
            best_index = 1;
        }
    }
    if (get_key_valid(get_key_entry(store, 2)) == KEY_VALID) {
        if (ts >= best_timestamp) {
            best_timestamp = ts;
            best_index = 2;
        }
    }
    if (get_key_valid(get_key_entry(store, 3)) == KEY_VALID) {
        if (ts >= best_timestamp) {
            best_timestamp = ts;
            best_index = 3;
        }
    }
    if (best_index != 0xFF) {
        return get_key_value(get_key_entry(store, best_index));
    }
    return 0;
}
fn count_valid_keys(store: u64) u32 {
    if (get_key_valid(get_key_entry(store, 0)) == KEY_VALID) {
        count = count + 1;
    }
    if (get_key_valid(get_key_entry(store, 1)) == KEY_VALID) {
        count = count + 1;
    }
    if (get_key_valid(get_key_entry(store, 2)) == KEY_VALID) {
        count = count + 1;
    }
    if (get_key_valid(get_key_entry(store, 3)) == KEY_VALID) {
        count = count + 1;
    }
    return count;
}
test "create_key_entry_basic" {
    entry = create_key_entry(1, 5, 0xABCD, 100);
    if (!(get_key_valid(entry) == 1)) @compileError("assertion failed");
    if (!(get_key_id(entry) == 5)) @compileError("assertion failed");
    if (!(get_key_value(entry) == 0xABCD)) @compileError("assertion failed");
    if (!(get_key_timestamp(entry) == 100)) @compileError("assertion failed");
}
test "find_key_by_id_found" {
    store = create_key_store(create_key_entry(1, 1, 0x1111, 10), create_key_entry(1, 2, 0x2222, 20), create_key_entry(0, 3, 0x3333, 30), create_key_entry(1, 4, 0x4444, 40));
    if (!(find_key_by_id(store, 2) == 1)) @compileError("assertion failed");
}
test "find_key_by_id_not_found" {
    store = create_key_store(create_key_entry(1, 1, 0x1111, 10), create_key_entry(1, 2, 0x2222, 20), create_key_entry(0, 3, 0x3333, 30), create_key_entry(1, 4, 0x4444, 40));
    if (!(find_key_by_id(store, 99) == 0xFF)) @compileError("assertion failed");
}
test "find_key_by_id_invalid_ignored" {
    store = create_key_store(create_key_entry(1, 1, 0x1111, 10), create_key_entry(0, 2, 0x2222, 20), create_key_entry(1, 3, 0x3333, 30), create_key_entry(1, 4, 0x4444, 40));
    if (!(find_key_by_id(store, 2) == 0xFF)) @compileError("assertion failed");
}
test "add_key_empty_slot" {
    store = create_key_store(create_key_entry(0, 0, 0, 0), create_key_entry(0, 0, 0, 0), create_key_entry(0, 0, 0, 0), create_key_entry(0, 0, 0, 0));
    new_store = add_key(store, 5, 0xABCD, 100);
    if (!(get_key_id(get_key_entry(new_store, 0)) == 5)) @compileError("assertion failed");
}
test "invalidate_key_works" {
    store = create_key_store(create_key_entry(1, 1, 0x1111, 10), create_key_entry(1, 2, 0x2222, 20), create_key_entry(1, 3, 0x3333, 30), create_key_entry(0, 0, 0, 0));
    new_store = invalidate_key(store, 2);
    if (!(get_key_valid(get_key_entry(new_store, 1)) == 0)) @compileError("assertion failed");
}
test "needs_rotation_true" {
    entry = create_key_entry(1, 1, 0x1111, 1000);
    if (!(needs_rotation(entry, 35000) == true)) @compileError("assertion failed");
}
test "needs_rotation_false" {
    entry = create_key_entry(1, 1, 0x1111, 25000);
    if (!(needs_rotation(entry, 30000) == false)) @compileError("assertion failed");
}
test "needs_rotation_invalid" {
    entry = create_key_entry(0, 1, 0x1111, 1000);
    if (!(needs_rotation(entry, 35000) == false)) @compileError("assertion failed");
}
test "rotate_key_works" {
    store = create_key_store(create_key_entry(1, 1, 0x1111, 10000), create_key_entry(0, 0, 0, 0), create_key_entry(0, 0, 0, 0), create_key_entry(0, 0, 0, 0));
    new_store = rotate_key(store, 1, 0x9999, 50000);
    if (!(get_key_value(get_key_entry(new_store, 0)) == 0x9999)) @compileError("assertion failed");
}
test "get_active_key_returns_latest" {
    store = create_key_store(create_key_entry(1, 1, 0x1111, 10), create_key_entry(1, 2, 0x2222, 30), create_key_entry(1, 3, 0x3333, 20), create_key_entry(0, 0, 0, 0));
    if (!(get_active_key(store) == 0x2222)) @compileError("assertion failed");
}
test "count_valid_keys_all" {
    store = create_key_store(create_key_entry(1, 1, 0x1111, 10), create_key_entry(1, 2, 0x2222, 20), create_key_entry(1, 3, 0x3333, 30), create_key_entry(1, 4, 0x4444, 40));
    if (!(count_valid_keys(store) == 4)) @compileError("assertion failed");
}
test "count_valid_keys_some" {
    store = create_key_store(create_key_entry(1, 1, 0x1111, 10), create_key_entry(0, 2, 0x2222, 20), create_key_entry(1, 3, 0x3333, 30), create_key_entry(0, 4, 0x4444, 40));
    if (!(count_valid_keys(store) == 2)) @compileError("assertion failed");
}
