// Generated from t27 spec: HealthMonitoring (module name)
// DO NOT EDIT — generated by t27c
// phi^2 + 1/phi^2 = 3 | TRINITY

const std = @import("std");

const types = @import("types.zig");

const MAX_CHECKS: u32 = 8;
const HEALTH_CRITICAL: u32 = 0;
const HEALTH_WARNING: u32 = 1;
const HEALTH_HEALTHY: u32 = 2;
const CHECK_INTERVAL: u32 = 1000;
fn create_health_check(check_type: u32, result: u32, value: u32, timestamp: u32) u32 {
    return ((((check_type & 0xF) << 28) | ((result & 0x3) << 26)) | ((value & 0xFF) << 8)) | (timestamp & 0xFF);
}
fn get_check_type(check: u32) u32 {
    return (check >> 28) & 0xF;
}
fn get_check_result(check: u32) u32 {
    return (check >> 26) & 0x3;
}
fn get_check_value(check: u32) u32 {
    return (check >> 8) & 0xFF;
}
fn get_check_timestamp(check: u32) u32 {
    return check & 0xFF;
}
const CHECK_CPU: u32 = 0;
const CHECK_MEMORY: u32 = 1;
const CHECK_DISK: u32 = 2;
const CHECK_NETWORK: u32 = 3;
const CHECK_TEMPERATURE: u32 = 4;
const CHECK_POWER: u32 = 5;
const CHECK_CONNECTIVITY: u32 = 6;
const CHECK_PROCESS: u32 = 7;
const RESULT_PASS: u32 = 0;
const RESULT_WARN: u32 = 1;
const RESULT_FAIL: u32 = 2;
const RESULT_SKIP: u32 = 3;
fn create_health_array(c0: u32, c1: u32, c2: u32, c3: u32, c4: u32, c5: u32, c6: u32, c7: u32) u64 {
    return (((((((@as(u64, @intCast(c0)) << 56) | (@as(u64, @intCast(c1)) << 48)) | (@as(u64, @intCast(c2)) << 40)) | (@as(u64, @intCast(c3)) << 32)) | (@as(u64, @intCast(c4)) << 24)) | (@as(u64, @intCast(c5)) << 16)) | (@as(u64, @intCast(c6)) << 8)) | @as(u64, @intCast(c7));
}
fn get_health_check(array: u64, index: u32) u32 {
    if (index == 0) {
        return @as(u32, @intCast((array >> 56) & 0xFFFFFFFF));
    }
    if (index == 1) {
        return @as(u32, @intCast((array >> 48) & 0xFFFFFFFF));
    }
    if (index == 2) {
        return @as(u32, @intCast((array >> 40) & 0xFFFFFFFF));
    }
    if (index == 3) {
        return @as(u32, @intCast((array >> 32) & 0xFFFFFFFF));
    }
    if (index == 4) {
        return @as(u32, @intCast((array >> 24) & 0xFFFFFFFF));
    }
    if (index == 5) {
        return @as(u32, @intCast((array >> 16) & 0xFFFFFFFF));
    }
    if (index == 6) {
        return @as(u32, @intCast((array >> 8) & 0xFFFFFFFF));
    }
    return @as(u32, @intCast(array & 0xFFFFFFFF));
}
fn update_health_check(array: u64, index: u32, new_check: u32) u64 {
    if (index == 0) {
        return (array & 0x00FFFFFFFFFFFFFF) | (@as(u64, @intCast(new_check)) << 56);
    } else if (index == 1) {
        return (array & 0xFF00FFFFFFFFFFFF) | (@as(u64, @intCast(new_check)) << 48);
    } else if (index == 2) {
        return (array & 0xFFFF00FFFFFFFFFF) | (@as(u64, @intCast(new_check)) << 40);
    } else if (index == 3) {
        return (array & 0xFFFFFF00FFFFFFFF) | (@as(u64, @intCast(new_check)) << 32);
    } else if (index == 4) {
        return (array & 0xFFFFFFFF00FFFFFF) | (@as(u64, @intCast(new_check)) << 24);
    } else if (index == 5) {
        return (array & 0xFFFFFFFFFF00FFFF) | (@as(u64, @intCast(new_check)) << 16);
    } else if (index == 6) {
        return (array & 0xFFFFFFFFFFFF00FF) | (@as(u64, @intCast(new_check)) << 8);
    } else {
        return (array & 0xFFFFFFFFFFFFFF00) | @as(u64, @intCast(new_check));
    }
}
fn calculate_overall_health(array: u64) u32 {
    if (get_check_result(get_health_check(array, 0)) == RESULT_FAIL) {
        failed = failed + 1;
    }
    if (get_check_result(get_health_check(array, 1)) == RESULT_FAIL) {
        failed = failed + 1;
    }
    if (get_check_result(get_health_check(array, 2)) == RESULT_FAIL) {
        failed = failed + 1;
    }
    if (get_check_result(get_health_check(array, 3)) == RESULT_FAIL) {
        failed = failed + 1;
    }
    if (get_check_result(get_health_check(array, 4)) == RESULT_FAIL) {
        failed = failed + 1;
    }
    if (get_check_result(get_health_check(array, 5)) == RESULT_FAIL) {
        failed = failed + 1;
    }
    if (get_check_result(get_health_check(array, 6)) == RESULT_FAIL) {
        failed = failed + 1;
    }
    if (get_check_result(get_health_check(array, 7)) == RESULT_FAIL) {
        failed = failed + 1;
    }
    if (get_check_result(get_health_check(array, 0)) == RESULT_WARN) {
        warnings = warnings + 1;
    }
    if (get_check_result(get_health_check(array, 1)) == RESULT_WARN) {
        warnings = warnings + 1;
    }
    if (get_check_result(get_health_check(array, 2)) == RESULT_WARN) {
        warnings = warnings + 1;
    }
    if (get_check_result(get_health_check(array, 3)) == RESULT_WARN) {
        warnings = warnings + 1;
    }
    if (get_check_result(get_health_check(array, 4)) == RESULT_WARN) {
        warnings = warnings + 1;
    }
    if (get_check_result(get_health_check(array, 5)) == RESULT_WARN) {
        warnings = warnings + 1;
    }
    if (get_check_result(get_health_check(array, 6)) == RESULT_WARN) {
        warnings = warnings + 1;
    }
    if (get_check_result(get_health_check(array, 7)) == RESULT_WARN) {
        warnings = warnings + 1;
    }
    if (failed > 0) {
        return HEALTH_CRITICAL;
    } else if (warnings >= 3) {
        return HEALTH_WARNING;
    } else {
        return HEALTH_HEALTHY;
    }
}
fn count_failed_checks(array: u64) u32 {
    if (get_check_result(get_health_check(array, 0)) == RESULT_FAIL) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 1)) == RESULT_FAIL) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 2)) == RESULT_FAIL) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 3)) == RESULT_FAIL) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 4)) == RESULT_FAIL) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 5)) == RESULT_FAIL) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 6)) == RESULT_FAIL) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 7)) == RESULT_FAIL) {
        count = count + 1;
    }
    return count;
}
fn count_warning_checks(array: u64) u32 {
    if (get_check_result(get_health_check(array, 0)) == RESULT_WARN) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 1)) == RESULT_WARN) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 2)) == RESULT_WARN) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 3)) == RESULT_WARN) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 4)) == RESULT_WARN) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 5)) == RESULT_WARN) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 6)) == RESULT_WARN) {
        count = count + 1;
    }
    if (get_check_result(get_health_check(array, 7)) == RESULT_WARN) {
        count = count + 1;
    }
    return count;
}
fn is_check_failing(array: u64, check_type: u32) bool {
    if ((check_type == CHECK_CPU) and (get_check_type(get_health_check(array, 0)) == CHECK_CPU)) {
        return get_check_result(get_health_check(array, 0)) == RESULT_FAIL;
    } else if ((check_type == CHECK_MEMORY) and (get_check_type(get_health_check(array, 1)) == CHECK_MEMORY)) {
        return get_check_result(get_health_check(array, 1)) == RESULT_FAIL;
    }
    return false;
}
fn get_health_percentage(array: u64) u32 {
    if (get_check_result(get_health_check(array, 0)) != RESULT_FAIL) {
        passing = passing + 1;
    }
    if (get_check_result(get_health_check(array, 0)) != RESULT_SKIP) {
        total = total + 1;
    }
    if (get_check_result(get_health_check(array, 1)) != RESULT_FAIL) {
        passing = passing + 1;
    }
    if (get_check_result(get_health_check(array, 1)) != RESULT_SKIP) {
        total = total + 1;
    }
    if (get_check_result(get_health_check(array, 2)) != RESULT_FAIL) {
        passing = passing + 1;
    }
    if (get_check_result(get_health_check(array, 2)) != RESULT_SKIP) {
        total = total + 1;
    }
    if (get_check_result(get_health_check(array, 3)) != RESULT_FAIL) {
        passing = passing + 1;
    }
    if (get_check_result(get_health_check(array, 3)) != RESULT_SKIP) {
        total = total + 1;
    }
    if (get_check_result(get_health_check(array, 4)) != RESULT_FAIL) {
        passing = passing + 1;
    }
    if (get_check_result(get_health_check(array, 4)) != RESULT_SKIP) {
        total = total + 1;
    }
    if (get_check_result(get_health_check(array, 5)) != RESULT_FAIL) {
        passing = passing + 1;
    }
    if (get_check_result(get_health_check(array, 5)) != RESULT_SKIP) {
        total = total + 1;
    }
    if (get_check_result(get_health_check(array, 6)) != RESULT_FAIL) {
        passing = passing + 1;
    }
    if (get_check_result(get_health_check(array, 6)) != RESULT_SKIP) {
        total = total + 1;
    }
    if (get_check_result(get_health_check(array, 7)) != RESULT_FAIL) {
        passing = passing + 1;
    }
    if (get_check_result(get_health_check(array, 7)) != RESULT_SKIP) {
        total = total + 1;
    }
    if (total == 0) {
        return 100;
    }
    return (passing * 100) / total;
}
test "create_health_check_basic" {
    check = create_health_check(CHECK_CPU, RESULT_PASS, 50, 100);
    if (!(get_check_type(check) == CHECK_CPU)) @compileError("assertion failed");
    if (!(get_check_result(check) == RESULT_PASS)) @compileError("assertion failed");
    if (!(get_check_value(check) == 50)) @compileError("assertion failed");
    if (!(get_check_timestamp(check) == 100)) @compileError("assertion failed");
}
test "calculate_overall_health_critical" {
    array = create_health_array(create_health_check(CHECK_CPU, RESULT_PASS, 50, 100), create_health_check(CHECK_MEMORY, RESULT_FAIL, 90, 101), create_health_check(CHECK_DISK, RESULT_PASS, 40, 102), create_health_check(CHECK_NETWORK, RESULT_PASS, 60, 103), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0));
    if (!(calculate_overall_health(array) == HEALTH_CRITICAL)) @compileError("assertion failed");
}
test "calculate_overall_health_warning" {
    array = create_health_array(create_health_check(CHECK_CPU, RESULT_WARN, 70, 100), create_health_check(CHECK_MEMORY, RESULT_WARN, 80, 101), create_health_check(CHECK_DISK, RESULT_WARN, 75, 102), create_health_check(CHECK_NETWORK, RESULT_PASS, 60, 103), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0));
    if (!(calculate_overall_health(array) == HEALTH_WARNING)) @compileError("assertion failed");
}
test "calculate_overall_health_healthy" {
    array = create_health_array(create_health_check(CHECK_CPU, RESULT_PASS, 50, 100), create_health_check(CHECK_MEMORY, RESULT_PASS, 40, 101), create_health_check(CHECK_DISK, RESULT_PASS, 45, 102), create_health_check(CHECK_NETWORK, RESULT_PASS, 35, 103), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0));
    if (!(calculate_overall_health(array) == HEALTH_HEALTHY)) @compileError("assertion failed");
}
test "count_failed_checks_multiple" {
    array = create_health_array(create_health_check(CHECK_CPU, RESULT_FAIL, 95, 100), create_health_check(CHECK_MEMORY, RESULT_FAIL, 90, 101), create_health_check(CHECK_DISK, RESULT_PASS, 40, 102), create_health_check(CHECK_NETWORK, RESULT_FAIL, 85, 103), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0));
    if (!(count_failed_checks(array) == 3)) @compileError("assertion failed");
}
test "count_warning_checks_multiple" {
    array = create_health_array(create_health_check(CHECK_CPU, RESULT_WARN, 70, 100), create_health_check(CHECK_MEMORY, RESULT_WARN, 75, 101), create_health_check(CHECK_DISK, RESULT_PASS, 40, 102), create_health_check(CHECK_NETWORK, RESULT_WARN, 72, 103), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0));
    if (!(count_warning_checks(array) == 3)) @compileError("assertion failed");
}
test "update_health_check_works" {
    array = create_health_array(create_health_check(CHECK_CPU, RESULT_PASS, 50, 100), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0));
    new_array = update_health_check(array, 1, create_health_check(CHECK_MEMORY, RESULT_FAIL, 90, 200));
    if (!(get_check_result(get_health_check(new_array, 1)) == RESULT_FAIL)) @compileError("assertion failed");
}
test "get_health_percentage_all_pass" {
    array = create_health_array(create_health_check(CHECK_CPU, RESULT_PASS, 50, 100), create_health_check(CHECK_MEMORY, RESULT_PASS, 40, 101), create_health_check(CHECK_DISK, RESULT_PASS, 45, 102), create_health_check(CHECK_NETWORK, RESULT_PASS, 35, 103), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0));
    if (!(get_health_percentage(array) == 100)) @compileError("assertion failed");
}
test "get_health_percentage_some_fail" {
    array = create_health_array(create_health_check(CHECK_CPU, RESULT_FAIL, 95, 100), create_health_check(CHECK_MEMORY, RESULT_PASS, 40, 101), create_health_check(CHECK_DISK, RESULT_FAIL, 90, 102), create_health_check(CHECK_NETWORK, RESULT_PASS, 35, 103), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0), create_health_check(0, 0, 0, 0));
    if (!(get_health_percentage(array) == 50)) @compileError("assertion failed");
}
